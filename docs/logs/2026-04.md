# 2026-04

## 2026-04-01 Eval 光谱图平滑与 TMM 精度配置
- 需求：优化 eval 逐样本光谱图的观感，不再直接连原始折线；需要按光谱点做平滑处理，并且强度坐标范围固定为 `0-1`。同时将 TMM 精度控制项写入 `configs/eval/spectrum_eval.yaml`。
- 文件：utils/plotting.py, evaluators/spectrum_evaluator.py, configs/eval/spectrum_eval.yaml, docs/notes.md, docs/logs/2026-04.md
- 修改：
  - `utils/plotting.py`
    - 为 `save_spectrum_comparison_plot(...)` 新增平滑插值逻辑
    - 默认使用 `PCHIP` 做形状保持插值
    - 新增 `smooth_curves / smoothing_method / smoothing_upsample_factor / show_original_points / clip_to_unit_interval`
    - 将每个子图的 y 轴固定到 `0.0 ~ 1.0`
  - `evaluators/spectrum_evaluator.py`
    - 从 `evaluation.plot_smoothing` 读取配置，并在保存逐样本图时传入绘图函数
  - `configs/eval/spectrum_eval.yaml`
    - 新增 `evaluation.plot_smoothing` 配置段
    - 在 `tmm` 段补充 `complex_dtype: complex64` 及注释
- 验证：
  - `D:\anaconda\envs\oneday\python.exe -m compileall utils evaluators`
  - `@' ... save_spectrum_comparison_plot(...) smoke test ... '@ | D:\anaconda\envs\oneday\python.exe -`
- 结果：通过
- 提交：`git commit -m "feat: smooth eval spectrum plots and expose tmm precision"`

## 2026-04-11 our_work 数据生成与预训练设计
- 需求：为新的 `2-15um` 光谱数据生成与预训练体系整理正式设计，要求与根目录现有强化学习路径解耦，并为后续独立重写的 GRPO 预留清晰接口。
- 文件：docs/superpowers/specs/2026-04-11-our-work-spectral-pretrain-design.md, docs/notes.md, docs/logs/2026-04.md
- 修改：
  - `docs/superpowers/specs/2026-04-11-our-work-spectral-pretrain-design.md`
    - 新增完整设计文档
    - 明确 `our_work/_shared / data_gen / pretrain` 的目录边界
    - 确认数据生成方案为 `5-10` 层各 `50w`、总计 `300w`
    - 确认预训练方案为 `Transformers + Trainer + decoder-only + 光谱 projector 前缀`
    - 规定数据分片、tokenizer、checkpoint、未来 RL 接口约束
  - `docs/notes.md`
    - 覆盖为本次设计文档摘要
  - `docs/logs/2026-04.md`
    - 追加本次记录
- 验证：
  - 未验证
- 结果：已完成设计文档落盘
- 提交：`git commit -m "docs: add our_work data generation and pretraining design"`

## 2026-04-11 our_work 实现计划拆分
- 需求：将已确认的 `our_work` 设计拆成可执行实现计划。由于 `data_gen` 与 `pretrain` 依赖关系清晰但职责独立，因此分别落两份计划文档。
- 文件：docs/superpowers/plans/2026-04-11-our-work-data-generation.md, docs/superpowers/plans/2026-04-11-our-work-pretraining.md, docs/notes.md, docs/logs/2026-04.md
- 修改：
  - `docs/superpowers/plans/2026-04-11-our-work-data-generation.md`
    - 新增数据生成实现计划
    - 覆盖 `_shared` 迁移、材料 registry、采样、TMM 模拟、分片写入与 CLI
  - `docs/superpowers/plans/2026-04-11-our-work-pretraining.md`
    - 新增预训练实现计划
    - 覆盖 tokenizer、collator、自定义 HF 模型、generation/scoring 与 Trainer 入口
  - `docs/notes.md`
    - 覆盖为本次计划摘要
  - `docs/logs/2026-04.md`
    - 追加本次记录
- 验证：
  - 未验证
- 结果：已完成计划文档落盘
- 提交：`git commit -m "docs: add our_work implementation plans"`

## 2026-04-11 our_work 数据生成与预训练代码落地
- 需求：在 `our_work/` 下独立实现数据生成与预训练代码，先完成本地 smoke 级别验证，不依赖根目录现有强化学习链路。
- 文件：`our_work/data_gen/configs/dataset_v1.yaml`，`our_work/pretrain/`，`tests/our_work/pretrain/`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/data_gen/configs/dataset_v1.yaml`
    - 将 smoke 数据集改为 `1024` 个波长点，保证 `R+T` 输入维度为真实训练使用的 `2048`
  - `our_work/pretrain/dataset/`
    - 新增独立 tokenizer、parquet 记录加载与 prefix-aware collator
    - collator 支持 `numpy` 光谱输入且不再产生张量构造 warning
    - tokenizer 补齐 `save_pretrained()` / `from_pretrained()`
  - `our_work/pretrain/model/`
    - 新增 `SpectralGPTConfig`、`SpectrumProjector`、`SpectralGPTForCausalLM`
    - 新增 generation 与 teacher-forcing scoring helper
    - 修正 `Trainer` 训练时透传额外参数导致的 forward 兼容问题
  - `our_work/pretrain/scripts/`
    - 新增 `run_pretrain.py` 与 `run_eval.py`
    - 训练入口显式固定为 PyTorch-only
    - 通过窄兼容层处理仓库顶层 `datasets/` 包与 `transformers.Trainer` 的命名冲突
  - `our_work/pretrain/configs/`
    - 新增基础模型与训练 YAML
  - `tests/our_work/pretrain/`
    - 新增 tokenizer、collator、model forward、generation、training smoke 测试
    - training smoke 已覆盖 dataloader、单步训练与 checkpoint 保存
  - `docs/notes.md`
    - 覆盖为本次实现摘要
  - `docs/logs/2026-04.md`
    - 追加本次实现记录
- 验证：
  - `D:\anaconda\envs\oneday\python.exe -m pytest tests\our_work\shared tests\our_work\data_gen tests\our_work\pretrain -v`
  - `D:\anaconda\envs\oneday\python.exe our_work\data_gen\scripts\run_build_dataset.py --config our_work\data_gen\configs\dataset_v1.yaml`
  - `D:\anaconda\envs\oneday\python.exe our_work\pretrain\scripts\run_pretrain.py --model-config our_work\pretrain\configs\model\base_gpt.yaml --train-config our_work\pretrain\configs\train\base_train.yaml`
- 结果：已完成 `our_work` 独立预训练最小闭环，数据生成与单步训练 smoke 验证通过
- 提交：待提交

## 2026-04-11 our_work smoke 评测链路补齐
- 需求：补齐独立 smoke 评测入口，加载 checkpoint，生成结构 token，回算光谱并输出指标。
- 文件：`our_work/pretrain/scripts/run_eval.py`，`tests/our_work/pretrain/test_eval.py`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：新增 checkpoint/路径解析、逐样本生成、光谱回算、RMSE/MAE 汇总与 JSON 输出；补齐评测测试。
- 验证：`D:\anaconda\envs\oneday\python.exe -m pytest tests\our_work\shared tests\our_work\data_gen tests\our_work\pretrain -q`；`D:\anaconda\envs\oneday\python.exe our_work\pretrain\scripts\run_eval.py --checkpoint-dir our_work\pretrain\outputs\base_run --dataset-dir our_work\data_gen\outputs\v1_smoke --database-dir database --split val --max-samples 2 --max-new-tokens 10 --output-json our_work\pretrain\outputs\base_run\eval_smoke.json`
- 结果：本地 smoke checkpoint 已能产出 JSON 评测结果。
- 提交：待提交

## 2026-04-11 run_eval 批量落盘与可视化设计
- 需求：为 `our_work/pretrain/scripts/run_eval.py` 设计批量结果落盘与可视化规范。
- 文件：`docs/superpowers/specs/2026-04-11-run-eval-batch-visualization-design.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：新增输出目录结构、summary/results schema、图表集合、最差+随机样本抽样策略、分层统计与容错规则设计。
- 验证：未验证
- 结果：已完成批量评测输出设计文档。
- 提交：待提交

## 2026-04-11 run_eval 批量落盘与可视化实现计划
- 需求：将已确认的 `run_eval.py` 批量落盘与可视化设计拆成可执行实现计划。
- 文件：`docs/superpowers/plans/2026-04-11-run-eval-batch-visualization.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：新增结果输出、绘图、脚本集成、端到端 smoke 与文档同步四个任务的实现计划。
- 验证：未验证
- 结果：已完成批量评测可视化实现计划。
- 提交：待提交

## 2026-04-11 our_work eval output serialization helpers
- Request: add eval artifact serialization helpers for independent pretrain evaluation.
- Files: `our_work/pretrain/eval_outputs.py`, `tests/our_work/pretrain/test_eval_outputs.py`, `docs/notes.md`, `docs/logs/2026-04.md`
- Changes: added JSONL serialization, global and per-layer summary aggregation, and timestamped `eval_runs` directory creation; added matching tests.
- Verification: `pytest tests/our_work/pretrain/test_eval_outputs.py -v`
- Result: 3 passed
- Commit: pending

## 2026-04-11 feat-our-work-bootstrap pytest 入口收口
- 需求：继续完成 `.worktrees/feat-our-work-bootstrap` 中未收口的工作，修复 worktree 根目录下直接执行 `pytest` 无法导入 `our_work` 的问题，并同步最新文档记录。
- 文件：`tests/conftest.py`，`tests/test_pytest_entrypoint.py`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `tests/conftest.py`
    - 新增 pytest 路径引导，在 `pytest.exe` 入口下显式把 worktree 根目录加入 `sys.path`
  - `tests/test_pytest_entrypoint.py`
    - 新增子进程回归测试，直接覆盖 `pytest tests/our_work/pretrain/test_collator.py -q` 的导入场景
  - `docs/notes.md`
    - 覆盖为本次收口摘要
  - `docs/logs/2026-04.md`
    - 追加本次记录
- 验证：
  - `python -m pytest tests/test_pytest_entrypoint.py -q`
  - `pytest tests/test_pytest_entrypoint.py -q`
  - `pytest tests/our_work/pretrain -q`
- 结果：通过；worktree 根目录下可直接运行 `pytest` 收集并执行 `our_work/pretrain` 测试。
- 提交：待提交

## 2026-04-11 feat-our-work-bootstrap 提交前清理
- 需求：清理 `.worktrees/feat-our-work-bootstrap` 中遗留的临时评测 PNG，并把 worktree 保持到可直接提交、可同步回主工作区的状态。
- 文件：`tmp_review_nan_spectrum.png`，`tmp_review_sample_final.png`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - 删除 `tmp_review_nan_spectrum.png`
  - 删除 `tmp_review_sample_final.png`
  - 同步更新本次摘要与日志
- 验证：待后续与提交前整体验证一并执行
- 结果：待提交
- 提交：待提交

## 2026-04-12 our_work 服务器部署配置修复
- 需求：把 `our_work` 的默认配置从 smoke 改成服务器可直接运行的版本，并修复 `our_work` 入口脚本里对 YAML 内部路径与配置文件路径依赖当前工作目录的问题；同时显式校验 `num_points` 与 `model.spectrum_dim` 的一致性。
- 文件：`our_work/_shared/io/config.py`，`our_work/data_gen/scripts/run_build_dataset.py`，`our_work/pretrain/scripts/run_pretrain.py`，`our_work/pretrain/scripts/run_eval.py`，`our_work/data_gen/configs/dataset_v1.yaml`，`our_work/pretrain/configs/train/base_train.yaml`，`our_work/pretrain/configs/model/base_gpt.yaml`，`tests/our_work/shared/test_config.py`，`tests/our_work/pretrain/test_training_smoke.py`，`tests/our_work/pretrain/test_eval.py`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/_shared/io/config.py`
    - 新增仓库根路径解析与配置路径归一化工具
    - 支持从 worktree 向父目录回溯已存在路径
  - `our_work/data_gen/scripts/run_build_dataset.py`
    - `--config` 路径改为按仓库根解析
    - YAML 内部相对路径改为按仓库根解析
  - `our_work/pretrain/scripts/run_pretrain.py`
    - `--model-config` / `--train-config` 路径改为按仓库根解析
    - 新增训练前 `spectrum_dim` 与样本光谱长度一致性校验
  - `our_work/pretrain/scripts/run_eval.py`
    - 复用统一的仓库根路径解析
    - `--num-points` 默认从 checkpoint 的 `model.config.spectrum_dim` 自动推导
    - 新增 `num_points` 与 `spectrum_dim` 不一致时的显式报错
    - `--output-dir` / `--output-json` 相对路径改为按仓库根解析
    - 默认评测输出根目录调整为 `outputs/our_work/pretrain`
  - `our_work/data_gen/configs/dataset_v1.yaml`
    - `database_dir` 改为 `database`
    - 输出目录改为 `outputs/our_work/data_gen/v1`
    - 默认数据规模改为 `5-10` 层、每 bucket `50w`
    - 补充 `num_points` 与 `model.spectrum_dim` 约束注释
  - `our_work/pretrain/configs/train/base_train.yaml`
    - 数据与输出路径改为正式目录
    - 去掉 smoke 的 `max_steps: 1`
    - 默认训练超参数改为服务器训练参数
  - `our_work/pretrain/configs/model/base_gpt.yaml`
    - 补充 `spectrum_dim = 2 * num_points` 约束注释
  - `tests/our_work/shared/test_config.py`
    - 新增仓库根路径解析回归测试
  - `tests/our_work/pretrain/test_training_smoke.py`
    - 新增训练前维度校验测试
  - `tests/our_work/pretrain/test_eval.py`
    - 新增评测阶段 `num_points` 自动推导与报错测试
  - `docs/notes.md`
    - 覆盖为本次部署修复摘要
  - `docs/logs/2026-04.md`
    - 追加本次记录
- 验证：
  - `python -m compileall our_work tests/our_work`
  - `python -c "... helper-checks-ok ..."`
  - `python ..\data_gen\scripts\run_build_dataset.py --config .tmp_server_check\dataset_smoke.yaml`
    - 运行目录：`our_work/pretrain`
  - `python scripts\run_pretrain.py --model-config .tmp_server_check\model_smoke.yaml --train-config .tmp_server_check\train_smoke.yaml`
    - 运行目录：`our_work/pretrain`
  - `python ..\pretrain\scripts\run_eval.py --checkpoint-dir C:/Users/15450/.codex/memories/server-check/pretrain --dataset-dir C:/Users/15450/.codex/memories/server-check/data_gen --database-dir database --split val --max-samples 1 --max-new-tokens 8 --disable-plots --output-dir C:/Users/15450/.codex/memories/server-check/eval_runs --output-json C:/Users/15450/.codex/memories/server-check/eval/payload.json`
    - 运行目录：`our_work/data_gen`
- 结果：
  - helper 检查通过
  - 从 `our_work` 子目录启动的数据生成、训练、评测 smoke 通过
  - 本机 `pytest` 在 session 清理临时目录时存在权限异常，因此未得到干净退出码；新增测试已通过 `compileall` 与辅助断言校验
- 提交：待提交

## 2026-04-12 our_work README 部署指南与进度条补齐
- 需求：把 `our_work` 的服务器部署与运行指南正式写入 `README.md`，并检查 `our_work` 代码里缺失的长耗时进度反馈，补齐必要的进度条。
- 文件：`README.md`，`our_work/data_gen/pipeline/build_dataset.py`，`our_work/data_gen/scripts/run_build_dataset.py`，`our_work/data_gen/configs/dataset_v1.yaml`，`our_work/pretrain/scripts/run_eval.py`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `README.md`
    - 新增 `our_work 服务器部署与运行` 章节
    - 覆盖必须同步目录、依赖安装、关键配置约束、从零部署步骤、只评测已有 checkpoint 的步骤
    - 为每一步补充典型终端输出与产物目录说明
  - `our_work/data_gen/pipeline/build_dataset.py`
    - 新增按 layer bucket 显示的 `tqdm` 进度条
    - 新增当前层数、生成条数、有效条数、累计保留条数的 postfix
  - `our_work/data_gen/scripts/run_build_dataset.py`
    - 支持从 YAML `logging.show_progress_bar` 读取数据生成进度条开关
  - `our_work/data_gen/configs/dataset_v1.yaml`
    - 新增 `logging.show_progress_bar: true`
  - `our_work/pretrain/scripts/run_eval.py`
    - 新增按样本数显示的评测 `tqdm` 进度条
    - 新增 `--disable-progress`
    - 新增有效生成数、精确匹配数、最近样本 RMSE 的 postfix
  - `docs/notes.md`
    - 覆盖为本次 README 与进度条修改摘要
  - `docs/logs/2026-04.md`
    - 追加本次记录
- 验证：
  - `python -c "import tqdm; print(tqdm.__version__)"`
  - `python -m compileall README.md our_work tests/our_work`
  - 内联 smoke：调用 `build_small_dataset(..., show_progress=False)` 并检查 `splits/split_manifest.json` 与 `vocab/vocab.json`
- 结果：通过
- 提交：待提交
