# 2026-04

## 2026-04-01 Eval 光谱图平滑与 TMM 精度配置
- 需求：优化 eval 逐样本光谱图的观感，不再直接连原始折线；需要按光谱点做平滑处理，并且强度坐标范围固定为 `0-1`。同时将 TMM 精度控制项写入 `configs/eval/spectrum_eval.yaml`。
- 文件：utils/plotting.py, evaluators/spectrum_evaluator.py, configs/eval/spectrum_eval.yaml, docs/notes.md, docs/logs/2026-04.md
- 修改：
  - `utils/plotting.py`
    - 为 `save_spectrum_comparison_plot(...)` 新增平滑插值逻辑
    - 默认使用 `PCHIP` 做形状保持插值
    - 新增 `smooth_curves / smoothing_method / smoothing_upsample_factor / show_original_points / clip_to_unit_interval`
    - 将每个子图的 y 轴固定到 `0.0 ~ 1.0`
  - `evaluators/spectrum_evaluator.py`
    - 从 `evaluation.plot_smoothing` 读取配置，并在保存逐样本图时传入绘图函数
  - `configs/eval/spectrum_eval.yaml`
    - 新增 `evaluation.plot_smoothing` 配置段
    - 在 `tmm` 段补充 `complex_dtype: complex64` 及注释
- 验证：
  - `D:\anaconda\envs\oneday\python.exe -m compileall utils evaluators`
  - `@' ... save_spectrum_comparison_plot(...) smoke test ... '@ | D:\anaconda\envs\oneday\python.exe -`
- 结果：通过
- 提交：`git commit -m "feat: smooth eval spectrum plots and expose tmm precision"`

## 2026-04-11 our_work 数据生成与预训练设计
- 需求：为新的 `2-15um` 光谱数据生成与预训练体系整理正式设计，要求与根目录现有强化学习路径解耦，并为后续独立重写的 GRPO 预留清晰接口。
- 文件：docs/superpowers/specs/2026-04-11-our-work-spectral-pretrain-design.md, docs/notes.md, docs/logs/2026-04.md
- 修改：
  - `docs/superpowers/specs/2026-04-11-our-work-spectral-pretrain-design.md`
    - 新增完整设计文档
    - 明确 `our_work/_shared / data_gen / pretrain` 的目录边界
    - 确认数据生成方案为 `5-10` 层各 `50w`、总计 `300w`
    - 确认预训练方案为 `Transformers + Trainer + decoder-only + 光谱 projector 前缀`
    - 规定数据分片、tokenizer、checkpoint、未来 RL 接口约束
  - `docs/notes.md`
    - 覆盖为本次设计文档摘要
  - `docs/logs/2026-04.md`
    - 追加本次记录
- 验证：
  - 未验证
- 结果：已完成设计文档落盘
- 提交：`git commit -m "docs: add our_work data generation and pretraining design"`

## 2026-04-11 our_work 实现计划拆分
- 需求：将已确认的 `our_work` 设计拆成可执行实现计划。由于 `data_gen` 与 `pretrain` 依赖关系清晰但职责独立，因此分别落两份计划文档。
- 文件：docs/superpowers/plans/2026-04-11-our-work-data-generation.md, docs/superpowers/plans/2026-04-11-our-work-pretraining.md, docs/notes.md, docs/logs/2026-04.md
- 修改：
  - `docs/superpowers/plans/2026-04-11-our-work-data-generation.md`
    - 新增数据生成实现计划
    - 覆盖 `_shared` 迁移、材料 registry、采样、TMM 模拟、分片写入与 CLI
  - `docs/superpowers/plans/2026-04-11-our-work-pretraining.md`
    - 新增预训练实现计划
    - 覆盖 tokenizer、collator、自定义 HF 模型、generation/scoring 与 Trainer 入口
  - `docs/notes.md`
    - 覆盖为本次计划摘要
  - `docs/logs/2026-04.md`
    - 追加本次记录
- 验证：
  - 未验证
- 结果：已完成计划文档落盘
- 提交：`git commit -m "docs: add our_work implementation plans"`

## 2026-04-11 our_work 数据生成与预训练代码落地
- 需求：在 `our_work/` 下独立实现数据生成与预训练代码，先完成本地 smoke 级别验证，不依赖根目录现有强化学习链路。
- 文件：`our_work/data_gen/configs/dataset_v1.yaml`，`our_work/pretrain/`，`tests/our_work/pretrain/`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/data_gen/configs/dataset_v1.yaml`
    - 将 smoke 数据集改为 `1024` 个波长点，保证 `R+T` 输入维度为真实训练使用的 `2048`
  - `our_work/pretrain/dataset/`
    - 新增独立 tokenizer、parquet 记录加载与 prefix-aware collator
    - collator 支持 `numpy` 光谱输入且不再产生张量构造 warning
    - tokenizer 补齐 `save_pretrained()` / `from_pretrained()`
  - `our_work/pretrain/model/`
    - 新增 `SpectralGPTConfig`、`SpectrumProjector`、`SpectralGPTForCausalLM`
    - 新增 generation 与 teacher-forcing scoring helper
    - 修正 `Trainer` 训练时透传额外参数导致的 forward 兼容问题
  - `our_work/pretrain/scripts/`
    - 新增 `run_pretrain.py` 与 `run_eval.py`
    - 训练入口显式固定为 PyTorch-only
    - 通过窄兼容层处理仓库顶层 `datasets/` 包与 `transformers.Trainer` 的命名冲突
  - `our_work/pretrain/configs/`
    - 新增基础模型与训练 YAML
  - `tests/our_work/pretrain/`
    - 新增 tokenizer、collator、model forward、generation、training smoke 测试
    - training smoke 已覆盖 dataloader、单步训练与 checkpoint 保存
  - `docs/notes.md`
    - 覆盖为本次实现摘要
  - `docs/logs/2026-04.md`
    - 追加本次实现记录
- 验证：
  - `D:\anaconda\envs\oneday\python.exe -m pytest tests\our_work\shared tests\our_work\data_gen tests\our_work\pretrain -v`
  - `D:\anaconda\envs\oneday\python.exe our_work\data_gen\scripts\run_build_dataset.py --config our_work\data_gen\configs\dataset_v1.yaml`
  - `D:\anaconda\envs\oneday\python.exe our_work\pretrain\scripts\run_pretrain.py --model-config our_work\pretrain\configs\model\base_gpt.yaml --train-config our_work\pretrain\configs\train\base_train.yaml`
- 结果：已完成 `our_work` 独立预训练最小闭环，数据生成与单步训练 smoke 验证通过
- 提交：待提交

## 2026-04-11 our_work smoke 评测链路补齐
- 需求：补齐独立 smoke 评测入口，加载 checkpoint，生成结构 token，回算光谱并输出指标。
- 文件：`our_work/pretrain/scripts/run_eval.py`，`tests/our_work/pretrain/test_eval.py`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：新增 checkpoint/路径解析、逐样本生成、光谱回算、RMSE/MAE 汇总与 JSON 输出；补齐评测测试。
- 验证：`D:\anaconda\envs\oneday\python.exe -m pytest tests\our_work\shared tests\our_work\data_gen tests\our_work\pretrain -q`；`D:\anaconda\envs\oneday\python.exe our_work\pretrain\scripts\run_eval.py --checkpoint-dir our_work\pretrain\outputs\base_run --dataset-dir our_work\data_gen\outputs\v1_smoke --database-dir database --split val --max-samples 2 --max-new-tokens 10 --output-json our_work\pretrain\outputs\base_run\eval_smoke.json`
- 结果：本地 smoke checkpoint 已能产出 JSON 评测结果。
- 提交：待提交

## 2026-04-11 run_eval 批量落盘与可视化设计
- 需求：为 `our_work/pretrain/scripts/run_eval.py` 设计批量结果落盘与可视化规范。
- 文件：`docs/superpowers/specs/2026-04-11-run-eval-batch-visualization-design.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：新增输出目录结构、summary/results schema、图表集合、最差+随机样本抽样策略、分层统计与容错规则设计。
- 验证：未验证
- 结果：已完成批量评测输出设计文档。
- 提交：待提交

## 2026-04-11 run_eval 批量落盘与可视化实现计划
- 需求：将已确认的 `run_eval.py` 批量落盘与可视化设计拆成可执行实现计划。
- 文件：`docs/superpowers/plans/2026-04-11-run-eval-batch-visualization.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：新增结果输出、绘图、脚本集成、端到端 smoke 与文档同步四个任务的实现计划。
- 验证：未验证
- 结果：已完成批量评测可视化实现计划。
- 提交：待提交

## 2026-04-11 our_work eval output serialization helpers
- Request: add eval artifact serialization helpers for independent pretrain evaluation.
- Files: `our_work/pretrain/eval_outputs.py`, `tests/our_work/pretrain/test_eval_outputs.py`, `docs/notes.md`, `docs/logs/2026-04.md`
- Changes: added JSONL serialization, global and per-layer summary aggregation, and timestamped `eval_runs` directory creation; added matching tests.
- Verification: `pytest tests/our_work/pretrain/test_eval_outputs.py -v`
- Result: 3 passed
- Commit: pending

## 2026-04-11 feat-our-work-bootstrap pytest 入口收口
- 需求：继续完成 `.worktrees/feat-our-work-bootstrap` 中未收口的工作，修复 worktree 根目录下直接执行 `pytest` 无法导入 `our_work` 的问题，并同步最新文档记录。
- 文件：`tests/conftest.py`，`tests/test_pytest_entrypoint.py`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `tests/conftest.py`
    - 新增 pytest 路径引导，在 `pytest.exe` 入口下显式把 worktree 根目录加入 `sys.path`
  - `tests/test_pytest_entrypoint.py`
    - 新增子进程回归测试，直接覆盖 `pytest tests/our_work/pretrain/test_collator.py -q` 的导入场景
  - `docs/notes.md`
    - 覆盖为本次收口摘要
  - `docs/logs/2026-04.md`
    - 追加本次记录
- 验证：
  - `python -m pytest tests/test_pytest_entrypoint.py -q`
  - `pytest tests/test_pytest_entrypoint.py -q`
  - `pytest tests/our_work/pretrain -q`
- 结果：通过；worktree 根目录下可直接运行 `pytest` 收集并执行 `our_work/pretrain` 测试。
- 提交：待提交

## 2026-04-11 feat-our-work-bootstrap 提交前清理
- 需求：清理 `.worktrees/feat-our-work-bootstrap` 中遗留的临时评测 PNG，并把 worktree 保持到可直接提交、可同步回主工作区的状态。
- 文件：`tmp_review_nan_spectrum.png`，`tmp_review_sample_final.png`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - 删除 `tmp_review_nan_spectrum.png`
  - 删除 `tmp_review_sample_final.png`
  - 同步更新本次摘要与日志
- 验证：待后续与提交前整体验证一并执行
- 结果：待提交
- 提交：待提交

## 2026-04-12 our_work 服务器部署配置修复
- 需求：把 `our_work` 的默认配置从 smoke 改成服务器可直接运行的版本，并修复 `our_work` 入口脚本里对 YAML 内部路径与配置文件路径依赖当前工作目录的问题；同时显式校验 `num_points` 与 `model.spectrum_dim` 的一致性。
- 文件：`our_work/_shared/io/config.py`，`our_work/data_gen/scripts/run_build_dataset.py`，`our_work/pretrain/scripts/run_pretrain.py`，`our_work/pretrain/scripts/run_eval.py`，`our_work/data_gen/configs/dataset_v1.yaml`，`our_work/pretrain/configs/train/base_train.yaml`，`our_work/pretrain/configs/model/base_gpt.yaml`，`tests/our_work/shared/test_config.py`，`tests/our_work/pretrain/test_training_smoke.py`，`tests/our_work/pretrain/test_eval.py`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/_shared/io/config.py`
    - 新增仓库根路径解析与配置路径归一化工具
    - 支持从 worktree 向父目录回溯已存在路径
  - `our_work/data_gen/scripts/run_build_dataset.py`
    - `--config` 路径改为按仓库根解析
    - YAML 内部相对路径改为按仓库根解析
  - `our_work/pretrain/scripts/run_pretrain.py`
    - `--model-config` / `--train-config` 路径改为按仓库根解析
    - 新增训练前 `spectrum_dim` 与样本光谱长度一致性校验
  - `our_work/pretrain/scripts/run_eval.py`
    - 复用统一的仓库根路径解析
    - `--num-points` 默认从 checkpoint 的 `model.config.spectrum_dim` 自动推导
    - 新增 `num_points` 与 `spectrum_dim` 不一致时的显式报错
    - `--output-dir` / `--output-json` 相对路径改为按仓库根解析
    - 默认评测输出根目录调整为 `outputs/our_work/pretrain`
  - `our_work/data_gen/configs/dataset_v1.yaml`
    - `database_dir` 改为 `database`
    - 输出目录改为 `outputs/our_work/data_gen/v1`
    - 默认数据规模改为 `5-10` 层、每 bucket `50w`
    - 补充 `num_points` 与 `model.spectrum_dim` 约束注释
  - `our_work/pretrain/configs/train/base_train.yaml`
    - 数据与输出路径改为正式目录
    - 去掉 smoke 的 `max_steps: 1`
    - 默认训练超参数改为服务器训练参数
  - `our_work/pretrain/configs/model/base_gpt.yaml`
    - 补充 `spectrum_dim = 2 * num_points` 约束注释
  - `tests/our_work/shared/test_config.py`
    - 新增仓库根路径解析回归测试
  - `tests/our_work/pretrain/test_training_smoke.py`
    - 新增训练前维度校验测试
  - `tests/our_work/pretrain/test_eval.py`
    - 新增评测阶段 `num_points` 自动推导与报错测试
  - `docs/notes.md`
    - 覆盖为本次部署修复摘要
  - `docs/logs/2026-04.md`
    - 追加本次记录
- 验证：
  - `python -m compileall our_work tests/our_work`
  - `python -c "... helper-checks-ok ..."`
  - `python ..\data_gen\scripts\run_build_dataset.py --config .tmp_server_check\dataset_smoke.yaml`
    - 运行目录：`our_work/pretrain`
  - `python scripts\run_pretrain.py --model-config .tmp_server_check\model_smoke.yaml --train-config .tmp_server_check\train_smoke.yaml`
    - 运行目录：`our_work/pretrain`
  - `python ..\pretrain\scripts\run_eval.py --checkpoint-dir C:/Users/15450/.codex/memories/server-check/pretrain --dataset-dir C:/Users/15450/.codex/memories/server-check/data_gen --database-dir database --split val --max-samples 1 --max-new-tokens 8 --disable-plots --output-dir C:/Users/15450/.codex/memories/server-check/eval_runs --output-json C:/Users/15450/.codex/memories/server-check/eval/payload.json`
    - 运行目录：`our_work/data_gen`
- 结果：
  - helper 检查通过
  - 从 `our_work` 子目录启动的数据生成、训练、评测 smoke 通过
  - 本机 `pytest` 在 session 清理临时目录时存在权限异常，因此未得到干净退出码；新增测试已通过 `compileall` 与辅助断言校验
- 提交：待提交

## 2026-04-12 our_work README 部署指南与进度条补齐
- 需求：把 `our_work` 的服务器部署与运行指南正式写入 `README.md`，并检查 `our_work` 代码里缺失的长耗时进度反馈，补齐必要的进度条。
- 文件：`README.md`，`our_work/data_gen/pipeline/build_dataset.py`，`our_work/data_gen/scripts/run_build_dataset.py`，`our_work/data_gen/configs/dataset_v1.yaml`，`our_work/pretrain/scripts/run_eval.py`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `README.md`
    - 新增 `our_work 服务器部署与运行` 章节
    - 覆盖必须同步目录、依赖安装、关键配置约束、从零部署步骤、只评测已有 checkpoint 的步骤
    - 为每一步补充典型终端输出与产物目录说明
  - `our_work/data_gen/pipeline/build_dataset.py`
    - 新增按 layer bucket 显示的 `tqdm` 进度条
    - 新增当前层数、生成条数、有效条数、累计保留条数的 postfix
  - `our_work/data_gen/scripts/run_build_dataset.py`
    - 支持从 YAML `logging.show_progress_bar` 读取数据生成进度条开关
  - `our_work/data_gen/configs/dataset_v1.yaml`
    - 新增 `logging.show_progress_bar: true`
  - `our_work/pretrain/scripts/run_eval.py`
    - 新增按样本数显示的评测 `tqdm` 进度条
    - 新增 `--disable-progress`
    - 新增有效生成数、精确匹配数、最近样本 RMSE 的 postfix
  - `docs/notes.md`
    - 覆盖为本次 README 与进度条修改摘要
  - `docs/logs/2026-04.md`
    - 追加本次记录
- 验证：
  - `python -c "import tqdm; print(tqdm.__version__)"`
  - `python -m compileall README.md our_work tests/our_work`
  - 内联 smoke：调用 `build_small_dataset(..., show_progress=False)` 并检查 `splits/split_manifest.json` 与 `vocab/vocab.json`
- 结果：通过
- 提交：待提交

## 2026-04-12 our_work 厚度配置改为区间
- 需求：把 `our_work/data_gen/configs/dataset_v1.yaml` 的厚度配置从显式列表改成区间描述，默认使用 `10nm-500nm`、步长 `10nm`，同时保持对旧列表写法的兼容。
- 文件：`our_work/data_gen/scripts/run_build_dataset.py`，`our_work/data_gen/configs/dataset_v1.yaml`，`tests/our_work/data_gen/test_build_dataset.py`，`README.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/data_gen/scripts/run_build_dataset.py`
    - 新增 `resolve_thickness_values_nm(...)`
    - 支持 `thickness_values_nm` 与 `thickness_range_nm` 两种配置格式
    - 新增冲突配置、非法步长、非法范围的显式校验
  - `our_work/data_gen/configs/dataset_v1.yaml`
    - 删除显式 `thickness_values_nm`
    - 改为 `thickness_range_nm: {min: 10, max: 500, step: 10}`
  - `tests/our_work/data_gen/test_build_dataset.py`
    - 新增区间展开测试
    - 新增冲突配置/非法步长/非法范围测试
  - `README.md`
    - 在 `our_work` 默认配置说明里补充 `thickness_range_nm`
  - `docs/notes.md`
    - 覆盖为本次修改摘要
  - `docs/logs/2026-04.md`
    - 追加本次记录
- 验证：
  - `python -m compileall our_work/data_gen/scripts/run_build_dataset.py tests/our_work/data_gen/test_build_dataset.py`
  - `python -c "from pathlib import Path; import yaml; from our_work.data_gen.scripts.run_build_dataset import resolve_thickness_values_nm; cfg=yaml.safe_load(Path('our_work/data_gen/configs/dataset_v1.yaml').read_text(encoding='utf-8')); values=resolve_thickness_values_nm(cfg['data']); assert values[0]==10 and values[-1]==500 and len(values)==50; assert resolve_thickness_values_nm({'thickness_values_nm':[10,20,30]})==[10,20,30]; print(values[:5], values[-5:], len(values))"`
- 结果：通过
- 提交：待提交

## 2026-04-12 our_work data_gen GPU 分块采样设计
- 需求：为 `our_work/data_gen` 的“GPU 分块采样 + 分块 TMM + bucket 内全局严格唯一补采”方案落正式设计文档，固定后续实现边界与配置。
- 文件：`docs/superpowers/specs/2026-04-12-our-work-data-gen-gpu-batching-design.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `docs/superpowers/specs/2026-04-12-our-work-data-gen-gpu-batching-design.md`
    - 新增正式设计文档
    - 覆盖当前问题、选定方案、配置设计、模块职责、数据流、错误处理、测试策略与建议默认值
  - `docs/notes.md`
    - 覆盖为本次设计摘要
  - `docs/logs/2026-04.md`
    - 追加本次记录
- 验证：未验证
- 结果：设计文档已落盘，等待用户 review 后继续写实现计划与代码。
- 提交：待提交

## 2026-04-12 our_work data_gen GPU 分块采样实现
- 需求：为 `our_work/data_gen` 增加 `sampling.batch_size` 和 `tmm.batch_size`，把结构生成改为 GPU/CPU 可切换的批量采样，把数据生成改为按 chunk 分批做 TMM，保证 `50w / bucket` 不再试图一次性塞进显存/内存，同时保留 bucket 内全局严格唯一和 chunk / bucket 级进度反馈。
- 文件：`our_work/data_gen/scripts/run_build_dataset.py`，`our_work/data_gen/configs/dataset_v1.yaml`，`our_work/data_gen/pipeline/sampler.py`，`our_work/data_gen/pipeline/build_dataset.py`，`tests/our_work/data_gen/test_sampler.py`，`tests/our_work/data_gen/test_build_dataset.py`，`README.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/data_gen/scripts/run_build_dataset.py`
    - 新增 `resolve_data_gen_runtime_config(...)`
    - 统一解析 `sampling.device / sampling.batch_size / sampling.max_duplicate_retry / tmm.batch_size`
    - 入口改为把 batching 参数透传给 `build_small_dataset(...)`
  - `our_work/data_gen/configs/dataset_v1.yaml`
    - 新增：
      - `sampling.device: auto`
      - `sampling.batch_size: 65536`
      - `sampling.max_duplicate_retry: 1000`
      - `tmm.batch_size: 2048`
  - `our_work/data_gen/pipeline/sampler.py`
    - 新增 `resolve_sampling_device(...)`
    - 新增 `sample_structure_token_batch(...)`
    - 结构候选改为可按 batch 在 GPU/CPU 上张量化采样
  - `our_work/data_gen/pipeline/build_dataset.py`
    - `build_small_dataset(...)` 新增 batching 相关参数
    - 主流程改为 sampling batch 补采 + bucket 内全局去重 + 按 `tmm_batch_size` 分块送 TMM
    - 新增 `max_duplicate_retry` 保护
    - 进度条 postfix 新增 `bucket_kept / bucket_target / sample_batch / tmm_batch / duplicates_skipped / valid_kept`
  - `tests/our_work/data_gen/test_sampler.py`
    - 新增 batched sampler 形状与 token 合法性测试
    - 新增 CUDA 不可用回退 CPU 的测试
  - `tests/our_work/data_gen/test_build_dataset.py`
    - 新增 batching 配置解析测试
    - 新增 chunked TMM orchestration 测试，验证 `tmm_batch_size=2` 时 TMM 调用序列为 `[2, 1]`
  - `README.md`
    - 更新 `our_work` 部署指南，补充 `sampling.*` 与 `tmm.batch_size` 说明
    - 说明结构采样和 TMM 计算均改为分块执行
  - `docs/notes.md`
    - 覆盖为本次实现摘要
  - `docs/logs/2026-04.md`
    - 追加本次实现记录
- 验证：
  - `python -c "from pathlib import Path; import yaml; from our_work.data_gen.scripts.run_build_dataset import resolve_data_gen_runtime_config; cfg=yaml.safe_load(Path('our_work/data_gen/configs/dataset_v1.yaml').read_text(encoding='utf-8')); runtime=resolve_data_gen_runtime_config(cfg); assert runtime['thickness_values_nm'][0]==10; assert runtime['thickness_values_nm'][-1]==500; assert len(runtime['thickness_values_nm'])==50; assert runtime['sampling_device']=='auto'; assert runtime['sampling_batch_size']==65536; assert runtime['max_duplicate_retry']==1000; assert runtime['tmm_batch_size']==2048; print(runtime['thickness_values_nm'][:3], runtime['thickness_values_nm'][-3:], runtime['sampling_batch_size'], runtime['tmm_batch_size'])"`
  - `python -c "from our_work.data_gen.pipeline.sampler import sample_structure_token_batch; batch=sample_structure_token_batch(material_names=['Ge','SiO2'], thickness_values_nm=[10,20], layer_count=3, batch_size=4, device='cpu', rng_seed=7); assert len(batch)==4; assert all(len(tokens)==3 for tokens in batch); print('sampler-ok', batch[0])"`
  - 内联 chunk 验证：monkeypatch `sample_structure_token_batch` 与 `simulate_structure_batch`，断言 `build_small_dataset(..., tmm_batch_size=2)` 的 TMM 调用序列为 `[2, 1]`
  - 运行态 smoke：
    - 从 `our_work/pretrain` 子目录运行 `python ..\data_gen\scripts\run_build_dataset.py --config <temp-config>`
    - 成功生成 `split_manifest.json`、`vocab.json` 和 parquet 分片
- 结果：通过
- 提交：待提交

## 2026-04-12 README 数据生成 batching 示例补齐
- 需求：把 `README.md` 里 `our_work` 数据生成的示例命令更新成包含 `sampling.*` 和 `tmm.batch_size` 的最终版说明。
- 文件：`README.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `README.md`
    - 在 `Step 4: 生成数据集` 前新增配置核对命令
    - 新增默认配置片段，明确展示 `sampling.device / sampling.batch_size / sampling.max_duplicate_retry / tmm.batch_size`
    - 保留最终运行命令 `python our_work/data_gen/scripts/run_build_dataset.py --config our_work/data_gen/configs/dataset_v1.yaml`
  - `docs/notes.md`
    - 覆盖为本次 README 更新摘要
  - `docs/logs/2026-04.md`
    - 追加本次记录
- 验证：
  - `rg -n "sampling =|tmm.batch_size =|sampling.batch_size|max_duplicate_retry" README.md`
- 结果：通过
- 提交：待提交

## 2026-04-12 our_work A100 多卡适配与轻量 GRPO
- 需求：针对 `4/8 x A100 80G` 优化 `our_work` 的 GPU 利用率与并行能力，包括数据生成、预训练多卡适配，以及在 `our_work` 下新增轻量、训练就绪的 GRPO 子系统。
- 文件：`our_work/data_gen/configs/dataset_v1.yaml`，`our_work/data_gen/configs/a100_4gpu.yaml`，`our_work/data_gen/configs/a100_8gpu.yaml`，`our_work/data_gen/scripts/run_build_dataset.py`，`our_work/data_gen/pipeline/build_dataset.py`，`our_work/data_gen/pipeline/simulator.py`，`our_work/pretrain/scripts/run_pretrain.py`，`our_work/pretrain/configs/train/base_train.yaml`，`our_work/pretrain/configs/train/a100_4gpu.yaml`，`our_work/pretrain/configs/train/a100_8gpu.yaml`，`our_work/rl/`，`tests/our_work/data_gen/`，`tests/our_work/pretrain/test_training_smoke.py`，`tests/our_work/rl/`，`README.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/data_gen`
    - 新增 `sampling.device / sampling.batch_size / sampling.max_duplicate_retry`
    - 新增 `tmm.device / tmm.cpu_threads / tmm.batch_size`
    - 新增 `distributed.enabled / distributed.timeout_minutes / distributed.shard_mode`
    - 新增 `a100_4gpu.yaml` 和 `a100_8gpu.yaml`
    - `run_build_dataset.py` 新增 rank 层级 layer bucket 分配、per-rank manifest 写出与主进程合并
    - `build_dataset.py` 支持 per-rank shard prefix、split manifest name、只由主进程写 vocab
    - `simulator.py` 支持把 `device` 透传到底层 TMM
  - `our_work/pretrain`
    - `run_pretrain.py` 的 `TrainingArguments` 新增 gradient accumulation、dataloader workers/prefetch/persistent workers、bf16、tf32、ddp backend、save_total_limit 等参数
    - `base_train.yaml` 更新为多卡友好配置
    - 新增 `a100_4gpu.yaml` 与 `a100_8gpu.yaml`
  - `our_work/rl`
    - 新增轻量 GRPO 子系统：
      - `objective.py`
      - `dataset.py`
      - `policy.py`
      - `reward.py`
      - `trainer.py`
      - `scripts/run_grpo.py`
      - `configs/grpo/base_grpo.yaml`
      - `configs/grpo/a100_4gpu.yaml`
      - `configs/grpo/a100_8gpu.yaml`
    - 采用 `torchrun + DDP + 自定义 GRPOTrainer` 路线
    - 兼容 `our_work/pretrain` checkpoint、shard 数据集和 `our_work/_shared/physics` TMM reward
  - `tests`
    - `tests/our_work/data_gen/test_build_dataset.py` 增加分布式 layer bucket 分配和 manifest 合并测试
    - `tests/our_work/pretrain/test_training_smoke.py` 增加新 TrainingArguments 参数断言
    - 新增 `tests/our_work/rl/test_objective.py`、`test_policy.py`、`test_reward.py`
  - `README.md`
    - 补充 `our_work` 的 4 卡/8 卡数据生成命令
    - 补充 `our_work` 的 4 卡/8 卡预训练命令
    - 补充 `our_work` 轻量 GRPO 的单卡/4卡/8卡命令
  - `docs/notes.md`
    - 覆盖为本次实现摘要
  - `docs/logs/2026-04.md`
    - 追加本次记录
- 验证：
  - `python -m compileall README.md our_work tests/our_work`
  - `python -m pytest tests/our_work -q --basetemp C:/Users/15450/.codex/memories/pytest-our-work-full`
  - `python our_work/rl/scripts/run_grpo.py --config C:/Users/15450/.codex/memories/our-work-rl-smoke/config.yaml`
- 结果：通过
- 提交：待提交

## 2026-04-12 our_work 材料库读取修复
- 需求：排查并修复 `our_work/data_gen` 在服务器运行时出现的 TMM 溢出与非物理光谱问题；已知材料库 `.xlsx` 格式固定为“第一列波长（`um`），第二列 `n`，第三列 `k`”。
- 文件：`our_work/_shared/physics/optical_calculator.py`，`tests/our_work/shared/test_optical_calculator.py`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/_shared/physics/optical_calculator.py`
    - 修正 `load_material_data(...)`
    - `xlsx` 改为按 `header=None` 读取，并直接把前三列解释为 `um / n / k`
    - `csv` 保留表头识别逻辑；若未识别到表头，则回退为无表头三列 `um / n / k`
    - 删除“`max(wavelengths) > 50` 就自动 `/1000`”的错误启发式
  - `tests/our_work/shared/test_optical_calculator.py`
    - 新增无表头 `xlsx` 读取测试
    - 新增无表头 `csv` 读取测试
  - `docs/notes.md`
    - 覆盖为本次修复摘要
  - `docs/logs/2026-04.md`
    - 追加本次记录
- 根因：
  - 原逻辑默认把 `.xlsx` 第一行数值数据当作表头
  - 在未识别到列名时，又把“首列最大值 > 50”错误地当成 `nm` 判据
  - 对本来就是 `um` 且覆盖到长波的材料（如 `Al / SiO2 / HfO2 / Ta2O5 / ZnO`）误做 `/1000`
  - 导致插值到 `2-15um` 时变成远距离外推，`n/k` 异常放大，进而触发 TMM 溢出和非物理 `R/T`
- 验证：
  - `python -m compileall our_work/_shared/physics/optical_calculator.py tests/our_work/shared/test_optical_calculator.py`
  - `python -c "... load_material_data('SiO2.xlsx') / load_material_data('Ge.csv') ..."`
  - 材料插值对照检查：`Al.xlsx / ZnO.xlsx / HfO2.xlsx / Ta2O5.xlsx / SiO2.xlsx`
  - 随机批量 TMM 验证：`ok_count = 8 / 8`，`warning_count = 0`
  - 运行态 smoke：`python our_work/data_gen/scripts/run_build_dataset.py --config <temp-config>`，`database_dir: our_work/_shared/database`
- 结果：通过
- 提交：待提交

## 2026-04-12 our_work RL review 问题修复
- 需求：修复上一轮 `our_work/rl` code review 中指出的 4 个问题：checkpoint 字符串排序、`distributed.backend` 配置未生效、checkpoint 未保存恢复状态、训练阶段未显式切回 `train()`。
- 文件：`utils/dist.py`，`our_work/rl/scripts/run_grpo.py`，`our_work/rl/trainer.py`，`tests/our_work/rl/test_run_grpo.py`，`tests/our_work/rl/test_trainer.py`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `utils/dist.py`
    - `init_distributed(...)` 新增 `backend` 参数
    - 若显式传入 backend，则按配置值初始化进程组
  - `our_work/rl/scripts/run_grpo.py`
    - 新增 `resolve_checkpoint_dir(...)`
    - 按 `checkpoint-<int>` 数值顺序选择最新 checkpoint
    - `main(...)` 改为支持 `argv`
    - 显式把 `distributed.backend` 透传给分布式初始化
    - 支持 `training.resume_from_checkpoint`
  - `our_work/rl/trainer.py`
    - 新增 `scheduler`
    - 新增 `global_step / resume_epoch / resume_batch_index`
    - `_save_checkpoint(...)` 新增保存 `optimizer.pt / scheduler.pt / trainer_state.json`
    - 新增 `_load_checkpoint_state(...)`
    - 训练开始时显式调用 `self.raw_model.train()` 和 `self.model.train()`
    - `_evaluate(...)` 在评测后恢复 train mode
    - 支持从保存的 epoch / batch 位置恢复训练
  - `tests/our_work/rl/test_run_grpo.py`
    - 新增 checkpoint 数值排序测试
    - 新增 `distributed.backend` 透传测试
  - `tests/our_work/rl/test_trainer.py`
    - 新增 checkpoint 保存 optimizer / scheduler / trainer_state 测试
    - 新增 checkpoint 恢复训练进度测试
    - 新增 `_evaluate(...)` 后恢复 train mode 测试
  - `docs/notes.md`
    - 覆盖为本次修复摘要
  - `docs/logs/2026-04.md`
    - 追加本次记录
- 验证：
  - `python -m pytest tests/our_work/rl -q --basetemp C:/Users/15450/.codex/memories/pytest-our-work-rl-fixes`
  - `python -m pytest tests/our_work -q --basetemp C:/Users/15450/.codex/memories/pytest-our-work-after-rl-fixes`
- 结果：通过
- 提交：待提交

## 2026-04-12 our_work 数据集分析与可视化
- 需求：在 `our_work/data_gen` 里增加自动数据集分析与独立分析 CLI，覆盖两类分析：1）每层材料和厚度分布；2）光谱谱形覆盖度分析，支持自动在数据集生成后执行，并支持单独传参分析某个数据集目录或若干 shard 文件。
- 文件：`our_work/data_gen/analysis/`，`our_work/data_gen/scripts/run_analyze_dataset.py`，`our_work/data_gen/scripts/run_build_dataset.py`，`our_work/data_gen/configs/dataset_v1.yaml`，`our_work/data_gen/configs/a100_4gpu.yaml`，`our_work/data_gen/configs/a100_8gpu.yaml`，`tests/our_work/data_gen/test_analysis.py`，`tests/our_work/data_gen/test_build_dataset.py`，`our_work/_shared/utils/seed.py`，`README.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/data_gen/analysis/`
    - 新增：
      - `io.py`
      - `plots.py`
      - `structure_analysis.py`
      - `spectrum_analysis.py`
      - `pipeline.py`
      - `__init__.py`
    - 结构分析输出：
      - 材料按层热图
      - 厚度按层热图
      - 全局材料条形图
      - 全局厚度条形图
    - 光谱分析输出：
      - `R/T` 均值与标准差图
      - PCA 二维散点图
      - cluster 大小分布图
      - cluster 代表谱形图
      - JSON 摘要
  - `our_work/data_gen/scripts/run_analyze_dataset.py`
    - 新增独立 CLI
    - 支持 `--dataset-dir` 或多个 `--shard-path`
    - 支持 `--split`、`--output-dir`、`--device`、PCA/KMeans 参数开关
  - `our_work/data_gen/scripts/run_build_dataset.py`
    - 新增 `resolve_analysis_runtime_config(...)`
    - 数据集生成完成后默认自动调用 `analyze_dataset(...)`
  - `our_work/data_gen/configs/*.yaml`
    - 新增 `analysis` 配置段：
      - `enabled`
      - `auto_after_build`
      - `batch_size`
      - `scopes`
      - `structure.enabled`
      - `spectrum.device`
      - `spectrum.pca_components`
      - `spectrum.cluster_count`
      - `spectrum.cluster_fit_samples`
      - `spectrum.cluster_iterations`
      - `spectrum.scatter_max_points`
  - `tests/our_work/data_gen/test_analysis.py`
    - 新增 end-to-end 分析产物测试
    - 新增独立 CLI 测试
  - `tests/our_work/data_gen/test_build_dataset.py`
    - 新增分析配置解析测试
    - 新增自动触发分析测试
  - `our_work/_shared/utils/seed.py`
    - `set_global_seed(...)` 增加 `rank_offset`
  - `README.md`
    - 新增自动分析说明
    - 新增独立分析命令和输出说明
  - `docs/notes.md`
    - 覆盖为本次实现摘要
  - `docs/logs/2026-04.md`
    - 追加本次记录
- 验证：
  - `python -m compileall our_work/data_gen tests/our_work/data_gen README.md`
  - `python -m pytest tests/our_work/data_gen -q --basetemp C:/Users/15450/.codex/memories/pytest-data-analysis`
  - `python -m pytest tests/our_work -q --basetemp C:/Users/15450/.codex/memories/pytest-our-work-with-analysis`
- 结果：
  - `data_gen` 分析相关测试通过
  - 全量 `tests/our_work` 在当前 Windows 环境下命中了既有的 `pretrain` 多进程 DataLoader worker 启动问题，不属于本次分析功能本身
- 提交：待提交

## 2026-04-12 our_work 默认配置切到单卡 A100
- 需求：把 `our_work` 的默认/base 配置改成适配 `单卡 A100 80G + 16 CPU`，同时保留 4 卡 / 8 卡专用配置和多卡注释，方便后续切换。
- 文件：`our_work/data_gen/configs/dataset_v1.yaml`，`our_work/pretrain/configs/train/base_train.yaml`，`our_work/rl/configs/grpo/base_grpo.yaml`，`README.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/data_gen/configs/dataset_v1.yaml`
    - 默认单卡数据生成配置
    - `tmm.batch_size` 调整为 `4096`
    - `tmm.cpu_threads` 保持 `16`
    - 新增注释说明多卡请改用 `a100_4gpu.yaml / a100_8gpu.yaml`
  - `our_work/pretrain/configs/train/base_train.yaml`
    - 默认单卡预训练配置
    - `per_device_train_batch_size: 16`
    - `per_device_eval_batch_size: 16`
    - 保留 `gradient_accumulation_steps: 2`
    - 保留 distributed 段和多卡注释
  - `our_work/rl/configs/grpo/base_grpo.yaml`
    - 默认单卡 GRPO 配置
    - `training.per_device_batch_size: 16`
    - `rollout.batch_size: 512`
    - `scoring.batch_size: 1024`
    - `reward.tmm.batch_size: 4096`
    - 保留 distributed 段和多卡注释
  - `README.md`
    - 把“当前默认值”更新为“单卡 A100 80G + 16 CPU”
    - 同步更新默认 `tmm.batch_size`、单卡 pretrain batch、单卡 GRPO batch
    - 明确说明多卡请直接使用 `a100_4gpu.yaml / a100_8gpu.yaml`
  - `docs/notes.md`
    - 覆盖为本次修改摘要
  - `docs/logs/2026-04.md`
    - 追加本次记录
- 验证：
  - `python -c "import yaml, pathlib; print(yaml.safe_load(pathlib.Path('our_work/data_gen/configs/dataset_v1.yaml').read_text(encoding='utf-8'))['tmm']['batch_size']); print(yaml.safe_load(pathlib.Path('our_work/pretrain/configs/train/base_train.yaml').read_text(encoding='utf-8'))['training']['per_device_train_batch_size']); print(yaml.safe_load(pathlib.Path('our_work/rl/configs/grpo/base_grpo.yaml').read_text(encoding='utf-8'))['training']['per_device_batch_size'])"`
- 结果：通过
- 提交：待提交

## 2026-04-12 our_work 光谱分析 RAPIDS 优化
- 需求：优化 `our_work/data_gen` 的光谱分析速度，重点解决重复全量扫描、`to_pylist()` 造成的 Python 开销，以及 GPU 利用率几乎为 0 的问题。
- 文件：`our_work/data_gen/configs/dataset_v1.yaml`，`our_work/data_gen/configs/a100_4gpu.yaml`，`our_work/data_gen/configs/a100_8gpu.yaml`，`our_work/data_gen/analysis/io.py`，`our_work/data_gen/analysis/pipeline.py`，`our_work/data_gen/analysis/spectrum_analysis.py`，`our_work/data_gen/analysis/structure_analysis.py`，`our_work/data_gen/scripts/run_build_dataset.py`，`tests/our_work/data_gen/test_analysis.py`，`tests/our_work/data_gen/test_build_dataset.py`，`README.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/data_gen/configs/*.yaml`
    - 自动分析默认 `scopes` 改为 `[all]`
    - 单卡默认 `analysis.batch_size` 提升到 `8192`
    - 多卡默认 `analysis.batch_size` 提升到 `16384`
    - 新增：
      - `analysis.spectrum.engine: rapids`
      - `analysis.spectrum.pca_fit_samples: 50000`
      - `analysis.spectrum.save_split_analysis: false`
  - `our_work/data_gen/analysis/io.py`
    - 新增 RAPIDS 列式读取：
      - `iter_spectrum_frames(...)`
      - `extract_spectrum_matrix(...)`
    - 保留结构分析专用的轻量 batch 读取：
      - `iter_structure_batches(...)`
    - 避免光谱主路径使用 `to_pylist()`
  - `our_work/data_gen/analysis/pipeline.py`
    - `analyze_dataset(...)` 新增 `engine` 与 `pca_fit_samples`
    - 光谱分析改为吃 `frame_factory`
  - `our_work/data_gen/analysis/spectrum_analysis.py`
    - 改成 RAPIDS 两遍分析：
      - Pass 1：mean/std + PCA fit reservoir
      - Pass 2：PCA transform + 聚类统计 + representative 选择
    - PCA 改用 `cuml.decomposition.PCA`
    - KMeans 改用 `cuml.cluster.KMeans`
    - GPU 上保留主要矩阵计算
    - CPU 回传压缩为少量 scatter 点、cluster 统计和代表谱形
    - 若缺失 `cudf/cupy/cuml`，会明确报错
  - `our_work/data_gen/analysis/structure_analysis.py`
    - 改为吃结构列式 batch
  - `our_work/data_gen/scripts/run_build_dataset.py`
    - `resolve_analysis_runtime_config(...)` 新增：
      - `spectrum_engine`
      - `pca_fit_samples`
      - `save_split_analysis`
    - 自动分析默认只跑 `all`
  - `tests/our_work/data_gen/test_build_dataset.py`
    - 新增分析默认 scope 为 `[all]` 的回归测试
  - `tests/our_work/data_gen/test_analysis.py`
    - 更新为允许在无 RAPIDS 环境下跳过 GPU 分析测试
  - `README.md`
    - 更新默认分析配置为 `all-only + rapids`
    - 更新默认 `tmm.batch_size = 4096`
    - 更新自动分析说明与独立 CLI 使用说明
  - `docs/notes.md`
    - 覆盖为本次优化摘要
  - `docs/logs/2026-04.md`
    - 追加本次记录
- 验证：
  - `python -m compileall our_work/data_gen tests/our_work/data_gen README.md`
  - `python -m pytest tests/our_work/data_gen -q --basetemp C:/Users/15450/.codex/memories/pytest-data-analysis-rapids`
- 结果：
  - `data_gen` 分析测试通过
  - 本机无 RAPIDS/CUDA 的分析测试被跳过，属于预期行为
- 提交：待提交

## 2026-04-13 our_work RAPIDS 运行时隔离最小修复
- 需求：针对服务器上 `torch 2.5.1+cu124` 与 pip 安装的 RAPIDS `cu12.9.x` 同进程冲突问题，做低风险最小修复，避免把 `libcuml++.so` 一类动态库错误误报成“未安装依赖”。
- 文件：`our_work/data_gen/scripts/run_build_dataset.py`，`our_work/data_gen/analysis/pipeline.py`，`our_work/data_gen/analysis/spectrum_analysis.py`，`tests/our_work/data_gen/test_analysis.py`，`README.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/data_gen/scripts/run_build_dataset.py`
    - 删除顶层分析导入。
    - RAPIDS 自动分析改为子进程调用 `run_analyze_dataset.py`。
    - 非 RAPIDS 路径才在父进程内懒加载 `analyze_dataset(...)`。
  - `our_work/data_gen/analysis/pipeline.py`
    - `analyze_spectrum_distribution` 改为按需导入，结构分析不再提前触发 RAPIDS 导入。
  - `our_work/data_gen/analysis/spectrum_analysis.py`
    - 删除顶层 `import torch`。
    - `resolve_analysis_device("auto")` 改为优先使用 `cupy` 检查 GPU。
    - 保留 `_RAPIDS_IMPORT_ERROR`，分析入口抛错时透传原始异常链和错误文本。
  - `tests/our_work/data_gen/test_analysis.py`
    - 新增 `cupy` 设备判定回归测试。
    - 新增 RAPIDS 原始导入异常透传测试。
  - `README.md`
    - 补充说明：RAPIDS 自动分析默认只跑 `all`，且走子进程隔离以避免 `torch` / RAPIDS CUDA 运行时栈冲突。
- 验证：
  - `D:\\anaconda\\envs\\oneday\\python.exe -m compileall our_work/data_gen tests/our_work/data_gen README.md`
  - 手工 smoke：
    - `resolve_analysis_device("auto")` 基于 `cupy` 生效
    - RAPIDS 导入异常透传
    - `run_build_dataset.py` 自动分析参数改走子进程
- 结果：
  - 编译通过
  - 手工 smoke 通过
  - `pytest` 在当前 Windows 环境仍会被临时目录权限问题打断收尾，未拿到干净退出码
- 提交：待提交

## 2026-04-13 our_work pretrain 单进程 DDP 误触发修复
- 需求：修复 `our_work/pretrain/scripts/run_pretrain.py` 在单卡直接执行时，因为 `ddp_backend` 配置或脏的 `LOCAL_RANK/WORLD_SIZE` 环境变量而误触发 `accelerate` 分布式路径，最终报 `Default process group has not been initialized`。
- 文件：`our_work/pretrain/scripts/run_pretrain.py`，`tests/our_work/pretrain/test_training_smoke.py`，`README.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/pretrain/scripts/run_pretrain.py`
    - 新增 `_distributed_training_requested()`。
    - 新增 `_sanitize_single_process_distributed_env()`。
    - `build_trainer(...)` 现在只在真实多卡环境下传入 `ddp_backend` / `ddp_find_unused_parameters`。
    - 单进程路径会清理 `LOCAL_RANK`、`RANK`、`WORLD_SIZE`、`MASTER_ADDR`、`MASTER_PORT`。
  - `tests/our_work/pretrain/test_training_smoke.py`
    - 新增回归测试，覆盖“`WORLD_SIZE=1` 但残留 DDP 环境变量”时，构造 Trainer 不应再误走 DDP。
  - `README.md`
    - 补充说明 `base_train.yaml` 的 `distributed.*` 仅在 `torchrun` 多卡场景下生效。
  - `docs/notes.md`
    - 覆盖为本次修复摘要。
- 验证：
  - `D:\\anaconda\\envs\\oneday\\python.exe -m compileall our_work/pretrain tests/our_work/pretrain`
  - 手工 smoke：
    - 人为设置 `LOCAL_RANK=0`、`RANK=0`、`WORLD_SIZE=1`
    - 调用 `build_trainer(...)` 并传入 `ddp_backend='nccl'`
- 结果：
  - 编译通过
  - 手工 smoke 输出 `manual-pretrain-ddp-fix-ok`
  - `pytest` 在当前 Windows 环境仍会被临时目录权限问题打断收尾，未拿到干净退出码
- 提交：待提交

## 2026-04-13 our_work pretrain 评估阶段 tuple predictions 修复
- 需求：修复 `our_work/pretrain` 在训练推进到 `eval_steps/save_steps` 触发点后，于 `Trainer._maybe_log_save_evaluate(...)` 阶段崩溃的问题。
- 根因：
  - `compute_token_accuracy(...)` 假设 `predictions` 一定是纯 logits 数组。
  - 当前模型评估时会把 `past_key_values` 一起带入输出，`Trainer` 会把 predictions 组织成 `(logits, past_key_values, ...)` 形式。
  - metrics 对 tuple 直接做 `np.argmax(...)`，会在评估阶段报错。
- 文件：`our_work/pretrain/trainer/metrics.py`，`our_work/pretrain/model/modeling_spectral_gpt.py`，`tests/our_work/pretrain/test_training_smoke.py`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/pretrain/trainer/metrics.py`
    - `compute_token_accuracy(...)` 兼容 tuple/list predictions，只取第一个 logits 张量计算精度。
  - `our_work/pretrain/model/modeling_spectral_gpt.py`
    - 当存在 `labels` 时显式 `use_cache=False`，避免训练/评估路径无意义地产生 generation cache。
  - `tests/our_work/pretrain/test_training_smoke.py`
    - 新增 metrics 兼容 `(logits, past_key_values)` 的回归测试。
    - 新增带 `labels` 的模型前向 cache 关闭测试。
  - `docs/notes.md`
    - 覆盖为本次评估阶段修复摘要。
- 验证：
  - `D:\\anaconda\\envs\\oneday\\python.exe -m compileall our_work/pretrain tests/our_work/pretrain`
  - 手工 smoke：
    - 直接调用 `compute_token_accuracy(((logits, fake_past_key_values), labels))`
- 结果：
  - 编译通过
  - metrics 手工 smoke 输出 `metrics-tuple-ok`
  - 模型 `use_cache=False` 路径已通过编译检查，但本机独立 smoke 未完成
  - `pytest` 在当前 Windows 环境仍会被临时目录权限问题打断收尾
- 提交：待提交

## 2026-04-13 our_work pretrain 正式训练节奏调整
- 需求：按当前 `our_work` 数据规模调整 `base_train.yaml` 的 `logging_steps / eval_steps / save_steps`，避免全量 val 评估过于频繁，导致训练大部分时间卡在验证阶段。
- 背景：
  - 当前默认数据规模约为 `train ≈ 2700000`、`val ≈ 300000`
  - 单卡默认训练配置为 `per_device_train_batch_size: 16`、`gradient_accumulation_steps: 2`、`per_device_eval_batch_size: 16`
  - 由此训练一个 epoch 约 `84375` 个 optimizer steps，全量 val 评估一次约 `18750` 个 eval batches
  - 原配置 `eval_steps: 500` / `save_steps: 500` 会让验证频率远高于正式训练可接受范围
- 文件：`our_work/pretrain/configs/train/base_train.yaml`，`README.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/pretrain/configs/train/base_train.yaml`
    - `training.logging_steps: 50 -> 200`
    - `training.eval_steps: 500 -> 100000`
    - `training.save_steps: 500 -> 50000`
    - 新增 YAML 注释，说明这组值针对当前全量 `train/val` 规模
  - `README.md`
    - 同步更新 `base_train.yaml` 默认训练频率说明
  - `docs/notes.md`
    - 覆盖为本次配置调整摘要
- 验证：
  - `D:\\anaconda\\envs\\oneday\\python.exe -c "import yaml, pathlib; cfg=yaml.safe_load(pathlib.Path('our_work/pretrain/configs/train/base_train.yaml').read_text(encoding='utf-8')); print(cfg['training']['logging_steps'], cfg['training']['eval_steps'], cfg['training']['save_steps'])"`
- 结果：`200 100000 50000`
- 提交：待提交

## 2026-04-13 our_work pretrain 验证吞吐优化
- 需求：优化 `our_work/pretrain` 的验证吞吐，解决全量 val 阶段显著慢于训练的问题。
- 根因：
  - 训练侧按 optimizer step 计数，且有 `gradient_accumulation_steps: 2`，表面 `it/s` 不能直接和验证 batch `it/s` 对比。
  - 验证阶段此前会为 metrics 收集完整 logits，`Trainer` 会累计 `[batch, seq_len, vocab_size]` 级别的大张量，再统一计算 `token_accuracy`，成本很高。
  - 单卡默认 `per_device_eval_batch_size: 16` 对当前小模型偏保守。
- 文件：`our_work/pretrain/trainer/metrics.py`，`our_work/pretrain/scripts/run_pretrain.py`，`our_work/pretrain/configs/train/base_train.yaml`，`tests/our_work/pretrain/test_training_smoke.py`，`README.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/pretrain/trainer/metrics.py`
    - 新增 `preprocess_logits_for_metrics(...)`，先把 logits 转成 `argmax token ids`
    - `compute_token_accuracy(...)` 同时兼容 logits、tuple predictions 和已预处理 token ids
  - `our_work/pretrain/scripts/run_pretrain.py`
    - `Trainer(...)` 新增 `preprocess_logits_for_metrics=preprocess_logits_for_metrics`
  - `our_work/pretrain/configs/train/base_train.yaml`
    - `training.per_device_eval_batch_size: 16 -> 64`
    - 保持 `logging_steps: 200`、`eval_steps: 100000`、`save_steps: 50000`
  - `tests/our_work/pretrain/test_training_smoke.py`
    - 新增 logits 预处理回归测试
    - 新增 metrics 接收 token ids 的回归测试
    - build_trainer smoke 额外断言 Trainer 已挂上 `preprocess_logits_for_metrics`
  - `README.md`
    - 同步更新默认 eval batch
    - 补充说明验证阶段会先把 logits 预处理成 token ids 再做 metrics
- 验证：
  - `D:\\anaconda\\envs\\oneday\\python.exe -m compileall our_work/pretrain tests/our_work/pretrain`
  - `D:\\anaconda\\envs\\oneday\\python.exe -c "import yaml, pathlib; cfg=yaml.safe_load(pathlib.Path('our_work/pretrain/configs/train/base_train.yaml').read_text(encoding='utf-8')); print(cfg['training']['per_device_eval_batch_size'], cfg['training']['logging_steps'], cfg['training']['eval_steps'], cfg['training']['save_steps'])"`
  - 手工 smoke：
    - 直接调用 `preprocess_logits_for_metrics(...)` 和 `compute_token_accuracy(...)`
- 结果：
  - 编译通过
  - 配置读取结果：`64 200 100000 50000`
  - 手工 smoke 输出 `preprocess-and-metrics-ok`
- 提交：待提交

## 2026-04-14 our_work pretrain 默认模型调整到 1024x6x16
- 需求：按指定配置把 `our_work/pretrain` 的默认模型参数改成 `n_embd: 1024`、`n_layer: 6`、`n_head: 16`。
- 文件：`our_work/pretrain/configs/model/base_gpt.yaml`，`README.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/pretrain/configs/model/base_gpt.yaml`
    - `n_embd: 256 -> 1024`
    - `n_layer` 保持 `6`
    - `n_head: 8 -> 16`
    - 新增注释，说明 `1024 / 16 = 64`
  - `README.md`
    - 同步更新 `base_gpt.yaml` 默认模型值：
      - `prefix_length: 8`
      - `n_embd: 1024`
      - `n_layer: 6`
      - `n_head: 16`
  - `docs/notes.md`
    - 覆盖为本次模型配置调整摘要
- 说明：
  - 当前设计下，光谱仍通过 projector 编码成 `8` 个 prefix vectors；修改后等价于编码成 `8 x 1024` 的 prefix token 表示。
  - 这和参考模型更像“单个 1024 维光谱 embedding”的设计不同。
- 验证：
  - `D:\\anaconda\\envs\\oneday\\python.exe -c "import yaml, pathlib; cfg=yaml.safe_load(pathlib.Path('our_work/pretrain/configs/model/base_gpt.yaml').read_text(encoding='utf-8')); print(cfg['model']['n_embd'], cfg['model']['n_layer'], cfg['model']['n_head'])"`
- 结果：`1024 6 16`
- 提交：待提交

## 2026-04-14 our_work 1024x6x16 默认学习率调整
- 需求：在默认模型已调整为 `n_embd: 1024`、`n_layer: 6`、`n_head: 16` 的前提下，直接把默认训练学习率改成更适合这档模型的值，并给出当前参数量估算。
- 文件：`our_work/pretrain/configs/train/base_train.yaml`，`README.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/pretrain/configs/train/base_train.yaml`
    - `training.learning_rate: 5e-4 -> 1e-4`
    - 新增注释，说明该值针对默认 `1024/6/16` 模型
  - `README.md`
    - 同步更新默认学习率说明
    - 顺手把默认 `logging_steps` 说明同步到当前 YAML 的 `1000`
  - `docs/notes.md`
    - 覆盖为本次学习率与参数量摘要
- 参数量说明：
  - 按当前默认 token 设计 `vocab_size = 1204` 估算：
    - backbone: `76,845,056`
    - projector: `20,981,760`
    - lm_head: `1,232,896`
    - total: `99,059,712`
  - 说明：
    - 当前默认模型更接近 `100M` 档，不是 `60M`
    - projector 仍然是 `8 x 1024` prefix token 设计
- 验证：
  - `D:\\anaconda\\envs\\oneday\\python.exe -c "import yaml, pathlib; cfg=yaml.safe_load(pathlib.Path('our_work/pretrain/configs/train/base_train.yaml').read_text(encoding='utf-8')); print(cfg['training']['learning_rate'], cfg['training']['logging_steps'])"`
- 结果：`0.0001 1000`
- 提交：待提交

## 2026-04-14 our_work pretrain 实时监控与训练曲线可视化
- 需求：给 `our_work/pretrain` 增加训练过程中的实时可视化与持久化指标记录，采用 TensorBoard + JSONL/CSV + 训练结束自动 PNG 的方案，并尽量减少 GPU/CPU 间搬运。
- 文件：`our_work/pretrain/monitoring.py`，`our_work/pretrain/scripts/run_pretrain.py`，`our_work/pretrain/configs/train/base_train.yaml`，`tests/our_work/pretrain/test_training_smoke.py`，`README.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/pretrain/monitoring.py`
    - 新增 `PretrainVisualizationCallback`
    - 支持：
      - TensorBoard 标量写入
      - `metrics/train_metrics.jsonl`
      - `metrics/eval_metrics.jsonl`
      - `train_metrics.csv` / `eval_metrics.csv`
      - `train_loss.png`
      - `learning_rate.png`
      - `grad_norm.png`
      - `eval_loss.png`
      - `eval_token_accuracy.png`
      - `overview.png`
    - `SummaryWriter` 改成懒加载，避免模块导入阶段拉起重依赖
  - `our_work/pretrain/scripts/run_pretrain.py`
    - `Trainer(...)` 新增挂载 `PretrainVisualizationCallback`
    - 从 `monitoring.*` 读取开关
  - `our_work/pretrain/configs/train/base_train.yaml`
    - 新增 `monitoring:` 配置段：
      - `tensorboard`
      - `jsonl`
      - `csv`
      - `save_plots`
      - `plot_every_eval`
      - `flush_secs`
  - `tests/our_work/pretrain/test_training_smoke.py`
    - 新增 callback 回归测试，验证 JSONL/CSV 产物落盘
  - `README.md`
    - 增加监控输出目录说明
    - 增加 TensorBoard 启动命令
    - 增加 `tensorboard` 依赖说明
- 说明：
  - 实时可视化主入口是 TensorBoard
  - 可视化路径只记录标量和已预处理 token ids，不搬运完整 logits
- 验证：
  - `D:\\anaconda\\envs\\oneday\\python.exe -m compileall our_work/pretrain tests/our_work/pretrain README.md`
  - 手工 smoke：
    - 构造 `PretrainVisualizationCallback`
    - 写入一次 train log 和一次 eval metrics
  - `D:\\anaconda\\envs\\oneday\\python.exe -c "import yaml, pathlib; cfg=yaml.safe_load(pathlib.Path('our_work/pretrain/configs/train/base_train.yaml').read_text(encoding='utf-8')); print(cfg['monitoring'])"`
- 结果：
  - 编译通过
  - 手工 smoke 输出 `pretrain-monitoring-smoke-ok`
  - 监控配置读取结果：`{'tensorboard': True, 'jsonl': True, 'csv': True, 'save_plots': True, 'plot_every_eval': True, 'flush_secs': 10}`
- 提交：待提交

## 2026-04-14 our_work 4 卡预训练配置调整
- 需求：按 `64 CPU + 4 x A100` 重新整理 `our_work/pretrain/configs/train/a100_4gpu.yaml`，并切换到 `our_work/data_gen/configs/a100_4gpu.yaml` 产出的数据集。
- 文件：`our_work/pretrain/configs/train/a100_4gpu.yaml`，`README.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/pretrain/configs/train/a100_4gpu.yaml`
    - `data.dataset_dir: outputs/our_work/data_gen/a100_4gpu`
    - `data.vocab_path: outputs/our_work/data_gen/a100_4gpu/vocab/vocab.json`
    - `data.num_workers: 12`
      - `num_workers` 是每个 rank / 每张卡各自的 worker 数，不是总数
    - `training.per_device_train_batch_size: 512`
    - `training.per_device_eval_batch_size: 512`
    - `training.num_train_epochs: 100`
    - `training.learning_rate: 1e-4`
    - `training.logging_steps: 1000`
    - `training.eval_steps: 5000`
    - `training.save_steps: 5000`
    - `training.save_total_limit: 3`
    - 新增 `monitoring:` 配置段，与当前监控回调保持一致
  - `README.md`
    - 补充 4 卡训练配置要点说明
  - `docs/notes.md`
    - 覆盖为本次 4 卡配置调整摘要
- 说明：
  - 保留 `gradient_accumulation_steps: 2`
  - 当前 4 卡全局有效 train batch 为 `512 * 2 * 4 = 4096`
- 验证：
  - `D:\\anaconda\\envs\\oneday\\python.exe -c "import yaml, pathlib; cfg=yaml.safe_load(pathlib.Path('our_work/pretrain/configs/train/a100_4gpu.yaml').read_text(encoding='utf-8')); print(cfg['data']['dataset_dir']); print(cfg['data']['vocab_path']); print(cfg['data']['num_workers']); print(cfg['training']['per_device_train_batch_size'], cfg['training']['per_device_eval_batch_size'], cfg['training']['num_train_epochs'], cfg['training']['logging_steps'], cfg['training']['eval_steps'], cfg['training']['save_steps'], cfg['training']['save_total_limit']); print(cfg['monitoring'])"`
- 结果：
  - `outputs/our_work/data_gen/a100_4gpu`
  - `outputs/our_work/data_gen/a100_4gpu/vocab/vocab.json`
  - `12`
  - `512 512 100 1000 5000 5000 3`
  - `{'tensorboard': True, 'jsonl': True, 'csv': True, 'save_plots': True, 'plot_every_eval': True, 'flush_secs': 10}`
- 提交：待提交

## 2026-04-14 our_work pretrain 调度与梯度裁剪配置接线
- 需求：给训练入口真正接上 `max_grad_norm`、`lr_scheduler_type`、`warmup_ratio`，并在 4 卡训练配置中启用：
  - `max_grad_norm: 1.0`
  - `lr_scheduler_type: cosine`
  - `warmup_ratio: 0.01`
- 文件：`our_work/pretrain/scripts/run_pretrain.py`，`our_work/pretrain/configs/train/a100_4gpu.yaml`，`tests/our_work/pretrain/test_training_smoke.py`，`README.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/pretrain/scripts/run_pretrain.py`
    - `build_trainer(...)` 新增：
      - `lr_scheduler_type`
      - `warmup_ratio`
      - `max_grad_norm`
    - 这三个值现在会传入 `TrainingArguments`
    - `main()` 中从 `training.*` 读取这三个配置
  - `our_work/pretrain/configs/train/a100_4gpu.yaml`
    - 新增：
      - `lr_scheduler_type: cosine`
      - `warmup_ratio: 0.01`
      - `max_grad_norm: 1.0`
  - `tests/our_work/pretrain/test_training_smoke.py`
    - build_trainer smoke 新增断言：
      - `lr_scheduler_type`
      - `warmup_ratio`
      - `max_grad_norm`
  - `README.md`
    - 补充 4 卡训练配置要点中的 scheduler / warmup / grad clip 说明
- 说明：
  - 如果训练进程实际看到的 `/dev/shm` 已经是 500G，那么此前 `No space left on device` 至少不应再是“共享内存总量太小”的问题
  - 仍建议在训练 shell 中确认：
    - `df -h /dev/shm`
- 验证：
  - `D:\\anaconda\\envs\\oneday\\python.exe -m compileall our_work/pretrain tests/our_work/pretrain`
  - `D:\\anaconda\\envs\\oneday\\python.exe -c "import yaml, pathlib; cfg=yaml.safe_load(pathlib.Path('our_work/pretrain/configs/train/a100_4gpu.yaml').read_text(encoding='utf-8')); print(cfg['training']['lr_scheduler_type'], cfg['training']['warmup_ratio'], cfg['training']['max_grad_norm'])"`
  - 手工 smoke：
    - 构造最小 Trainer，并传入 `cosine / 0.01 / 1.0`
- 结果：
  - 编译通过
  - YAML 读取结果：`cosine 0.01 1.0`
  - 手工 smoke 输出 `sched-grad-smoke-ok`
- 提交：待提交

## 2026-04-15 our_work eval 模块实现
- 需求：在 `our_work` 下新增独立 `eval` 模块，通过 YAML 配置统一评估 train/val，随机抽样、批量生成结构、批量回算光谱、输出误差统计和样本级可视化。
- 文件：
  - `our_work/eval/__init__.py`
  - `our_work/eval/dataset.py`
  - `our_work/eval/metrics.py`
  - `our_work/eval/reports.py`
  - `our_work/eval/plots.py`
  - `our_work/eval/pipeline.py`
  - `our_work/eval/scripts/run_eval_suite.py`
  - `our_work/eval/configs/base_eval.yaml`
  - `tests/our_work/eval/test_eval_suite.py`
  - `README.md`
  - `docs/notes.md`
  - `docs/logs/2026-04.md`
- 修改：
  - 新增 `our_work/eval` 模块，支持：
    - checkpoint 加载
    - split shard 解析与 reservoir 随机抽样
    - train/val 同时评估
    - 批量结构生成
    - 分批 TMM 回算
    - RMSE/MAE/exact/valid 统计
    - best / worst / mean 样本选择
    - split 对比图与样本图
  - 新增 `our_work/eval/scripts/run_eval_suite.py`
    - CLI 只收 `--config`
  - 新增 `our_work/eval/configs/base_eval.yaml`
    - 默认配置路径、抽样数、推理 batch、TMM batch、可视化数量
  - 新增 `tests/our_work/eval/test_eval_suite.py`
    - 端到端 smoke，验证 summary、results、comparison plot、sample plot 产物
  - `README.md`
    - 增加 `our_work Eval Suite` 使用说明、配置项与输出目录
- 说明：
  - 优先复用已有：
    - `our_work/pretrain/model/generation.py`
    - `our_work/data_gen/pipeline/simulator.py`
  - GPU 上执行：
    - 模型推理
    - TMM 批量计算
  - CPU 上执行：
    - JSON/JSONL 报告
    - PNG 绘图
- 验证：
  - `D:\\anaconda\\envs\\oneday\\python.exe -m compileall our_work/eval tests/our_work/eval README.md`
  - `D:\\anaconda\\envs\\oneday\\python.exe -m pytest tests/our_work/eval/test_eval_suite.py -q`
  - 手工 smoke：
    - 构造最小 checkpoint / dataset / database
    - monkeypatch 结构生成与 TMM 回算
- 结果：
  - 编译通过
  - `pytest` 在当前 Windows 环境仍会被 session 收尾的临时目录权限问题打断
  - 手工 smoke 输出 `eval-suite-manual-ok`
- 提交：待提交

## 2026-04-15 our_work eval 默认路径切换到 4 卡产物
- 需求：把 `our_work/eval/configs/base_eval.yaml` 的默认读取路径切换到 4 卡训练产物和 4 卡数据集产物。
- 文件：`our_work/eval/configs/base_eval.yaml`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `paths.checkpoint_dir: outputs/our_work/pretrain/a100_4gpu`
  - `paths.dataset_dir: outputs/our_work/data_gen/a100_4gpu`
  - `paths.output_dir: outputs/our_work/eval/a100_4gpu`
- 说明：
  - 现在默认 `run_eval_suite.py` 会直接对应 4 卡训练 / 4 卡数据生成目录，不再默认指向单卡 `base_train` / `v1`
- 验证：未验证
- 提交：待提交

## 2026-04-15 our_work eval 不同长度预测结构按层数分桶做 TMM
- 需求：处理 `our_work/eval` 中预测结构长度不一致时不能直接混批做 TMM 的问题。
- 文件：`our_work/eval/pipeline.py`，`tests/our_work/eval/test_eval_suite.py`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/eval/pipeline.py`
    - `_evaluate_records(...)` 改为先按 `prediction_layer_count` 分桶
    - 每个桶内再按 `tmm_batch_size` 分批调用 `simulate_structure_batch(...)`
  - `tests/our_work/eval/test_eval_suite.py`
    - 新增回归测试，验证不同长度预测结构会拆成多个 TMM batch
- 说明：
  - `max_new_tokens` 只是“最多再生成多少个 token”
  - 最终 decode 后不同样本的预测结构长度仍可能不同
  - 现在这些样本会先按层数分桶，再做批量 TMM
- 验证：
  - `D:\\anaconda\\envs\\oneday\\python.exe -m compileall our_work/eval tests/our_work/eval`
  - `D:\\anaconda\\envs\\oneday\\python.exe -m pytest tests/our_work/eval/test_eval_suite.py -q`
  - 手工 smoke：
    - 构造最小数据库上下文
    - 直接调用 `_evaluate_records(...)`
    - 验证不同长度预测结构会拆成多个 TMM batch
- 结果：
  - 编译通过
  - `pytest` 在当前 Windows 环境仍会被 session 收尾的临时目录权限问题打断
  - 手工 smoke 输出 `eval-layer-bucket-smoke-ok`
- 提交：待提交

## 2026-04-15 our_work eval 更快的 shard 抽样模式
- 需求：给 `our_work/eval` 增加更快的抽样模式，允许只扫描前若干 shard 或随机选若干 shard，而不是总是扫描整个 split。
- 文件：`our_work/eval/dataset.py`，`our_work/eval/pipeline.py`，`our_work/eval/configs/base_eval.yaml`，`tests/our_work/eval/test_eval_suite.py`，`README.md`，`docs/notes.md`，`docs/logs/2026-04.md`
- 修改：
  - `our_work/eval/dataset.py`
    - 新增 `select_split_shard_paths(...)`
    - 支持：
      - `sample_mode: random`
      - `sample_mode: head_shards`
      - `sample_mode: shard_subset_random`
    - `sample_split_records(...)` 新增 `sample_mode / max_shards`
    - 返回值增加 `scanned_shard_count`
  - `our_work/eval/pipeline.py`
    - 主流程接入 `data.sample_mode` 和 `data.max_shards_per_split`
    - split 汇总新增 `sample_mode`、`scanned_shard_count`
  - `our_work/eval/configs/base_eval.yaml`
    - 默认改成：
      - `sample_mode: head_shards`
      - `max_shards_per_split.train: 32`
      - `max_shards_per_split.val: 8`
  - `tests/our_work/eval/test_eval_suite.py`
    - 新增 shard 选择模式测试
    - 端到端 smoke 补充验证 `sample_mode`
  - `README.md`
    - 增加 `sample_mode` / `max_shards_per_split` 说明
    - 增加三种模式的取舍描述
- 说明：
  - `random` 最准确，但最慢
  - `head_shards` 最快，但可能有顺序偏差
  - `shard_subset_random` 速度和代表性折中
- 验证：待验证
- 提交：待提交
