## 2026-05-02

- 修正 `our_work/ga` 的 seeded GA 搜索语义：
  - 不再在达到样本数量或门槛后提前停止。
  - 改为固定运行 `restart_count * generations_per_restart` 的预算驱动搜索。
  - 维护去重 top-K 候选池；新样本更优时替换当前较差样本。
- 修正 seed 厚度处理：
  - 仅对初始优秀 seed 中大于 `500 nm` 的层做预处理拆分。
  - `seed_thickness_values_nm()` 改为基于拆分后的合法结构提取厚度，避免把 `820/850/870 nm` 重新带回后续 GA 搜索空间。
- 增加 GA 进度显示：
  - `run_ga_dataset.py` 的 tqdm postfix 现在会显示当前 target 的 `restart/generation/kept/best/worst`。
- 同步更新：
  - `our_work/ga/configs/ga_seeded_absorbers.yaml`
  - `README.md`
  - `tests/our_work/ga/test_targets.py`
  - `tests/our_work/ga/test_search.py`
  - `tests/our_work/ga/test_run_ga_dataset.py`

## 2026-05-02

- 为 `our_work/ga` 做中等强度 GPU 提速改造：
  - GA 种群内部表示由 `list[list[str]]` 改为 GPU/CPU 通用 `torch.Tensor` 索引表示。
  - 交叉、变异、随机注入、父代选择、score 拼接主路径改为 tensor 实现。
  - 新增 `calculate_optical_properties_indexed_batch_torch()`，允许直接使用 `material_indices + thickness_nm` 进入批量 TMM。
  - 新增 evaluator 级材料折射率 bank 缓存，避免每个 batch 重复构造材料光学常数张量。
- 新增测试：
  - `tests/our_work/ga/test_indexed_tmm.py`
  - `tests/our_work/ga/test_search.py` 中的 tensor population 用例
- 验证：
  - `pytest tests/our_work/ga -q ...` 通过，`14 passed`
  - `run_ga_dataset.main(...)` 最小烟测通过，确认主入口已走新 tensor evaluator

## 2026-05-02

- 继续优化 `our_work/ga` 在达到 `max_samples_per_target` 之后的掉速问题：
  - 候选池 key 从 token tuple 改为数值 key。
  - 每个 chunk 在进入 TMM 前先做 GPU batch 内去重。
  - 池满后改为动态 cutoff：`min(acceptance_floor_mse, current_worst_mse)`。
  - 新增最差样本堆，替代全池 `max(...)` 扫描。
  - evaluator 只返回轻量候选，真正入池时才 materialize 为 `GAStructure`。
  - 池满后对本 batch 候选做上限裁剪，减少无意义的 CPU 去重和对象构造。
- 新增 / 更新测试：
  - `tests/our_work/ga/test_search.py`
  - 全部 `our_work/ga` 测试通过，`17 passed`
  - `run_ga_dataset.main(...)` 最小烟测通过，`ga opt smoke ok`

## 2026-05-03

- 扩展 `our_work/ga` 主入口的目标配置方式：
  - 同一个 `run_ga_dataset.py` 现在支持 `targets.tasks` 用户自定义任务列表。
  - 默认 3 个 seeded 任务不再只靠代码硬编码，也显式写入 `our_work/ga/configs/ga_seeded_absorbers.yaml`。
- 新增任务构造与校验逻辑：
  - `our_work/ga/targets.py` 新增 `DEFAULT_GA_TASK_SPECS`、`default_ga_task_specs()`、`build_ga_targets_from_task_specs()`。
  - 支持用 `bands` 定义目标吸收区间，未声明波段不进入 loss。
  - 支持有 `seed_tokens` 的任务先做初始 seed 厚度检查与拆分。
  - 支持无 `seed_tokens` 的任务按 `random_init` 随机生成合法初始结构。
  - 新增 `validate_seed_tokens()` 与 `collect_seed_thickness_values()`。
- 主入口同步更新：
  - `resolve_ga_runtime_config()` 改为从配置任务列表提取 seed 厚度。
  - `build_targets_from_config()` 改为统一走 `targets.tasks` 链路。
- 新增配置模板：
  - `our_work/ga/configs/ga_custom_tasks.yaml`
- 同步更新：
  - `README.md`
  - `tests/our_work/ga/test_targets.py`
  - `tests/our_work/ga/test_run_ga_dataset.py`

## 2026-05-04

- `our_work/data_gen/analysis` 新增独立配置文件 `our_work/data_gen/analysis/configs/base_analysis.yaml`，用于集中管理结构分析、光谱分析、聚类与可视化参数。
- `run_analyze_dataset.py` 新增 `--config`，并实现参数优先级 `CLI > YAML > 默认值`。
- 聚类保持 `KMeans` 不变，新增 `auto_k` 机制：
  - 支持候选 `k_candidates`；
  - 支持 `single_metric` 与 `weighted_rank` 选型策略；
  - 在输出 `spectrum_analysis.json` 记录每个 k 的指标和最终选型依据。
- 修复 `cluster_fit_samples` 之前未参与 fit 采样目标的问题。
- 结构分析画图参数改为可配置：
  - `top_material_count` 控制全局材料柱状图显示前 N 种材料；
  - `max_thickness_ticks` 控制厚度全局柱状图 x 轴刻度密度。

## 2026-05-04

- 将 `our_work/data_gen/analysis/configs/base_analysis.yaml` 改为详细中文注释版。
- 注释覆盖 `analysis/structure/spectrum/pca/clustering/selection/visualization` 各层字段。
- 明确关键参数语义与影响：
  - `pca.components`、`pca.fit_samples`
  - `clustering.fit_samples`、`clustering.max_iter`
  - `visualization.scatter_max_points`
- 注释中补充了 `clustering.fit_samples` 与 `pca.fit_samples` 在当前实现中的协同规则（取较大值作为拟合采样目标）。

## 2026-05-04

- `our_work/data_gen/analysis/configs/base_analysis.yaml` 中 `k_candidates` 更新为 `[16,24,32,40]`。
- `save_cluster_representative_plot()` 改为分页绘图：
  - 单张图最多 16 类（4x4）。
  - 超出部分自动输出 `spectrum_cluster_representatives_partXX.png`。
- `spectrum_analysis.json` 的 `artifacts.cluster_representatives` 改为列表，支持记录多页代表图。

## 2026-05-04

- 为 `our_work/data_gen/analysis` 增加进度条：
  - scope 级：`analysis scopes`
  - 结构分析：`structure:<scope>`
  - 光谱分析：`spectrum:<scope>:pass1`、`spectrum:<scope>:pass2`
  - auto_k 候选：`spectrum:auto_k`
- 进度条依赖 `tqdm`，缺失时自动降级，不影响执行。

## 2026-05-04

- `our_work/pretrain/scripts/run_pretrain.py` 新增继续微调能力：
  - `--init-checkpoint-dir` 支持加载已有 checkpoint 权重继续训练。
  - `training.resume_from_checkpoint` 支持从中断训练状态恢复（优化器/调度器/步数）。
  - 新增 CLI 数据集覆盖：`--dataset-dir`、`--vocab-path`、`--train-split`、`--eval-split`。
- 新增 checkpoint 目录解析逻辑，支持传 run 根目录或具体 `checkpoint-*`。
- 增加 checkpoint 与 vocab 的 `vocab_size` 一致性校验。

## 2026-05-04

- 新增 `our_work/pretrain/scripts/eval_each_shard.py`，支持对 `shard-*.parquet` 逐 shard 独立评测与独立可视化输出。
- 新增 `run_eval_ga_custom_checkpoint980_each_shard.sh`，默认评测：
  - checkpoint: `outputs/our_work/pretrain/a100_4gpu/checkpoint-980`
  - shards_dir: `outputs/our_work/data_gen/ga_custom_tasks/shards`
- 每个 shard 单独生成 `summary/results/plots/samples`，并在输出根目录写 `combined_summary.json` 汇总。

## 2026-05-04

- 修复 `eval_each_shard.py` 导入路径：补充 `PROJECT_ROOT` 到 `sys.path`，解决 `ModuleNotFoundError: our_work`。
- 重新以 ASCII 编码写入 `run_eval_ga_custom_checkpoint980_each_shard.sh`，避免 Linux 下 shebang 因 BOM/CRLF 失效。

## 2026-05-05

- `our_work/eval` 增加分布式评测能力（torchrun 多卡）：
  - 按 shard 在 rank 间分片；
  - 各 rank 本地评测后汇聚到 rank0 做统一汇总与出图；
  - 多卡时按 `LOCAL_RANK` 绑定 GPU 设备。
- `our_work/eval/dataset.py` 新增 `sample_records_from_shards()` 用于分片后抽样。
- 删除临时脚本 `our_work/pretrain/scripts/eval_each_shard.py`。
- `run_eval_ga_custom_checkpoint980_each_shard.sh` 改为走官方入口：
  `torchrun --nproc_per_node=4 our_work/eval/scripts/run_eval_suite.py --config ...`

## 2026-05-05

- 新增固定评测配置 `our_work/eval/configs/ga_custom_checkpoint980_4gpu.yaml`。
- `run_eval_ga_custom_checkpoint980_each_shard.sh` 改为直接引用固定配置，不再生成临时 `.tmp` YAML。

## 2026-05-05

- 修复 `our_work/eval` 多卡评测的 NCCL 设备冲突：
  - 在 `dist.init_process_group()` 前执行 `torch.cuda.set_device(local_rank)`；
  - 解决 `Duplicate GPU detected` / `ncclInvalidUsage` 初始化失败。

## 2026-05-05

- 修复 `our_work/eval` 多卡评测中 `tmm.device=auto` 导致的设备解析报错：
  - 多卡下自动解析到 `cuda:<LOCAL_RANK>`。
- 增加评测进度条：
  - split 级：`eval:rank<rank>`
  - 推理 batch 级：`eval:predict`
  - TMM chunk 级：`eval:tmm:<split>`

## 2026-05-05

- 新增单脚本 `our_work/eval/scripts/run_ga_target_inference_analysis.py`：
  - 读取 `ga_custom_tasks.yaml` 目标 bands；
  - 使用 `outputs/our_work/pretrain/a100_4gpu/checkpoint-980` 推理结构；
  - 每目标采样默认 1024；
  - TMM 批量回算并输出 R/T/A 最优曲线图 + 全样本误差分析图。
