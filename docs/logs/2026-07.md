## 2026-07-15

- 将 17 种具有完整 2-25 um 数据覆盖的材料 Excel 表更新为 refractiveindex.info 单一来源数据：
  `Ag, Al, Al2O3, Au, Cr, Cu, HfO2, MgF2, MgO, Si, SiO2, Ta2O5, Ti, TiO2, VO2, ZnO, ZnS`。
- 新增 `our_work/_shared/database/SOURCES_2_25.md`，记录官方数据库 commit、逐材料数据集和样品形态。
- `our_work/pso/targets.py` 新增六波段高/低吸收组合生成器，支持最大跳变数与全低目标过滤。
- `run_pso_dataset.py` 支持通过 `targets.binary_bands` 加载组合任务，同时保留旧固定目标和 Lorentzian 行为。
- 新增 `our_work/pso/configs/pso_2_25_binary_bands.yaml`：
  - 波段边界为 `[2, 3, 5, 8, 13, 16, 25] um`；
  - 排除全低且最多两次相邻跳变，共 31 个目标；
  - 仅使用上述 17 种完整覆盖材料；
  - TMM 保持 1024 点，输出仍为 2048 维 `[R..., T...]`。
- 验证：17 张工作簿覆盖与单调性检查通过；PSO targets 测试 `5 passed`；语法编译通过。
- 待服务器验证：本地 `oneday` 环境不含 torch，未运行完整 PSO/TMM 测试与 GPU 烟测。

## 2026-07-15

- 为 PSO 增加每目标全局最佳候选跟踪：每个 target/layer 搜索记录最低 MSE 结构，汇总时再跨层数选择最佳。
- 新增 `our_work/pso/visualization.py`：
  - 最佳候选按层数分桶批量 TMM 回算；
  - 每目标生成目标/最佳吸收与 R/T 光谱图；
  - 图内输出逐层材料和厚度表；
  - 保存单目标 JSON 与 `best_structures.json` 汇总。
- `pso_2_25_binary_bands.yaml` 默认启用 `visualization`，产物写入 `plots/best_targets`。
- `search_summary.json` 增加逐层最佳 MSE、结构 token 和 visualization manifest。
- 验证：目标与绘图测试 `7 passed`；1024 点、10 层结构 PNG 视觉检查通过；完整 GPU 搜索待服务器验证。

## 2026-07-15

- 调整 `pso_2_25_binary_bands.yaml`，将 2-25 um PSO 改为更偏向广度且更短预算的搜索：
  - 厚度范围扩为 `20-1000 nm / step 20`，保持 50 个离散厚度值；
  - population `8192 -> 16384`；
  - iterations `20 -> 8`；
  - restarts `5 -> 2`；
  - search/TMM batch `8192 -> 16384`；
  - acceptance MSE `0.01 -> 0.015`；
  - stagnant iterations `5 -> 3`。
- 最大候选评测预算由约 1.52 亿降至约 4876 万，减少约 68%，单 restart 初始覆盖扩大 2 倍。
- 保持 31 个目标、5-10 层、1024 波长点和 complex128 不变。
