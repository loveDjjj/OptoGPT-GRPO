# 2026-03

## 2026-03-27
- 需求：整理并收缩当前仓库文档体系，仅保留 README.md、AGENTS.md、docs/notes.md、docs/logs/2026-03.md，并合并旧文档中的有效信息。
- 文件：README.md, AGENTS.md, docs/notes.md, docs/logs/2026-03.md, TMM/README.md
- 修改：重写主 README，新增 agent 规则与本次记录，并移除重复的 TMM 子文档。
- 验证：`rg --files -g "*.md"`
- 结果：通过
- 提交：`git commit -m "docs: consolidate project documentation"`

## 2026-03-27 Spectral SFT 重构
- 需求：删除 RL/GRPO 主线，改为“光谱测评 + 光谱损失 SFT”主线，并迁移 TMM 与材料库目录。
- 文件：README.md, AGENTS.md, configs/eval/spectrum_eval.yaml, configs/sft/spectral_sft.yaml, runners/run_spectrum_eval.py, runners/run_spectral_sft.py, models/optogpt/*, datasets/*, evaluators/*, losses/*, physics/*, trainers/spectral_sft_trainer.py, utils/logging.py, utils/dist.py, utils/seed.py, scripts/merge_eval_results.py, scripts/pkl_to_npy.py, data/materials/*.csv；删除 run_grpo.py, configs/grpo_base.yaml, policy/, rewards/, rollouts/, trainers/grpo_trainer.py, data/spectrum_generator.py, utils/structure.py, TMM/, nk/
- 修改：新增光谱测评入口、多卡分布式运行支持、光谱损失训练器、OptoGPT 模型封装、数据集封装与新的项目目录结构；TMM 迁移到 physics，材料库迁移到 data/materials；补充 pkl -> npy 转换脚本。
- 验证：`@' import compileall ... '@ | python -`
- 结果：通过
- 提交：`git commit -m "refactor: replace grpo pipeline with spectral sft workflow"`

## 2026-03-27 NPY 与并行优化
- 需求：将 `dataset/Structure_train.pkl`、`dataset/Structure_test.pkl`、`dataset/Spectrum_train.pkl`、`dataset/Spectrum_test.pkl` 转换为 `.npy`，检查当前代码是否适配 `.npy`，并优化当前多卡训练的数据加载开销。
- 文件：README.md, configs/eval/spectrum_eval.yaml, configs/sft/spectral_sft.yaml, datasets/optogpt_dataset.py, datasets/splits.py, scripts/pkl_to_npy.py, evaluators/spectrum_evaluator.py, losses/spectrum_loss.py, trainers/spectral_sft_trainer.py, docs/notes.md, docs/logs/2026-03.md，以及新增的 `dataset/*.npy`
- 修改：
  - 实际生成 `dataset/Spectrum_train.npy`、`dataset/Spectrum_test.npy`、`dataset/Structure_train.npy`、`dataset/Structure_test.npy`
  - 数据加载层兼容 `.pkl/.npy`，数值型 `.npy` 优先使用内存映射
  - 光谱数组转换为 `float32`，降低磁盘、CPU 内存和多卡重复读取开销
  - spectral SFT 训练阶段新增 `skip_train_structure_loading`，避免每个 DDP rank 重复加载 `Structure_train.npy`
  - 补充 DataLoader `persistent_workers/prefetch_factor`、TMM `bucket_by_layer_count` 和训练阶段 `DDP no_sync()` 说明
  - README 和配置注释同步更新为 `.npy` 路径与 Windows/Gloo、Linux/NCCL 的多卡说明
- 验证：
  - `D:\anaconda\envs\oneday\python.exe -c "... build_split_datasets(...)"`，结果显示 `train_structure_loaded=false`、`val_structure_loaded=true`
  - `D:\anaconda\envs\oneday\python.exe -c "import compileall; ..."`，结果 `overall: True`
- 提交：`git commit -m "feat: add npy dataset support and reduce ddp training io"`

## 2026-03-27 Runner 导入与评测出图
- 需求：修复 `python runners/run_spectrum_eval.py --config ...` 直接运行时报 `ModuleNotFoundError: No module named 'datasets'`，并在评测阶段输出光谱对比图片。
- 文件：runners/run_spectrum_eval.py, runners/run_spectral_sft.py, evaluators/spectrum_evaluator.py, utils/plotting.py, configs/eval/spectrum_eval.yaml, README.md, docs/notes.md, docs/logs/2026-03.md
- 修改：
  - 在 runner 中补充仓库根目录到 `sys.path`，允许直接用 `python runners/...` 启动
  - 为评测器新增目标/生成光谱的 `R/T/A` 可视化图片输出
  - 在评测配置中新增 `save_plots` 与 `plot_max_samples`，默认开启出图并限制每个 rank 的图片数量
  - README 同步补充 `plots/<split>/rankXX/*.png` 输出目录
- 验证：
  - `D:\anaconda\envs\oneday\python.exe runners/run_spectrum_eval.py --help`
  - `D:\anaconda\envs\oneday\python.exe runners/run_spectral_sft.py --help`
  - `D:\anaconda\envs\oneday\python.exe -c "import compileall; ..."`
- 结果：通过
- 提交：`git commit -m "fix: support direct runners execution and eval plots"`

## 2026-03-27 R/T 光谱损失
- 需求：将 SFT 与光谱测评中的光谱损失从“吸收率误差”改为直接比较 `R/T` 光谱的误差。
- 文件：physics/spectrum.py, losses/spectrum_loss.py, configs/eval/spectrum_eval.yaml, configs/sft/spectral_sft.yaml, README.md, docs/notes.md, docs/logs/2026-03.md
- 修改：
  - 将默认光谱误差指标从 `absorption_rmse` 改为 `rt_rmse`
  - `rt_rmse` 直接比较拼接后的 `[R..., T...]` 光谱 RMSE
  - 保留 `absorption_rmse` 作为历史兼容路径
  - 额外显式支持 `r_rmse`、`t_rmse`、`rt_mae`
  - 评测与 SFT 配置同步切换到 `rt_rmse`
- 验证：
  - `D:\anaconda\envs\oneday\python.exe -c "from physics.spectrum import spectrum_error; ..."`
  - `D:\anaconda\envs\oneday\python.exe -c "import compileall; ..."`
- 结果：通过
- 提交：`git commit -m "feat: switch spectral loss to rt error"`

## 2026-03-27 吞吐优化与固定 20 层
- 需求：检查当前是否多进程加载，修复加载慢与显卡空闲问题；把 TMM 结构统一填充到 20 层并写入 YAML；提高 batch 和数据吞吐。
- 文件：datasets/collator.py, models/optogpt/checkpoint.py, models/optogpt/generation.py, physics/structure.py, losses/spectrum_loss.py, trainers/spectral_sft_trainer.py, evaluators/spectrum_evaluator.py, configs/sft/spectral_sft.yaml, configs/eval/spectrum_eval.yaml, docs/notes.md, docs/logs/2026-03.md
- 修改：
  - 确认当前 DataLoader 在 `num_workers > 0` 时就是多进程加载
  - collator 改为直接返回 `torch.Tensor`，让 `pin_memory` 真正生效
  - 模型侧增加 `non_blocking=True` 的批量搬运路径
  - 生成阶段补充 `torch.Tensor` batch 快路，减少 Python 列表拆装开销
  - 新增 `fixed_max_layers`，并把训练/评测配置固定为 `20`
  - 关闭 `bucket_by_layer_count`，减少分桶开销
  - 提高默认 batch / worker / prefetch 配置，优先改善单卡吞吐
- 验证：
  - `D:\anaconda\envs\oneday\python.exe -c "from datasets.collator ..."`，结果 `spectra` 和 `sample_indices` 均为 `Tensor`
  - `D:\anaconda\envs\oneday\python.exe -c "from losses.spectrum_loss import evaluate_generated_structures ..."`，结果返回 `too_many_layers>20`
  - `D:\anaconda\envs\oneday\python.exe -c "import compileall; ..."`，结果 `overall: True`
- 提交：`git commit -m "perf: increase throughput with fixed 20-layer padding"`

## 2026-03-27 Batch 进度条
- 需求：在训练和评测阶段增加按 DataLoader batch 数量统计的进度条显示。
- 文件：trainers/spectral_sft_trainer.py, evaluators/spectrum_evaluator.py, configs/sft/spectral_sft.yaml, configs/eval/spectrum_eval.yaml, docs/notes.md, docs/logs/2026-03.md
- 修改：
  - 训练阶段接入 `tqdm`，按 epoch 内 batch 总数显示进度
  - 评测阶段接入 `tqdm`，按 split 内 batch 总数显示进度
  - 只在主进程显示进度条，避免多卡重复输出
  - YAML 中新增 `logging.show_progress_bar: true`
- 验证：
  - `D:\anaconda\envs\oneday\python.exe -c "import tqdm; print(tqdm.__version__)"`
  - `D:\anaconda\envs\oneday\python.exe -c "import compileall; ..."`
- 结果：通过
- 提交：`git commit -m "feat: add batch-based tqdm progress bars"`

## 2026-03-27 评测统计图
- 需求：在评测阶段增加统计图，用直方图/热力图展示 `R-RMSE`、`T-RMSE`、生成长度 vs 真值长度、序列误差的分布关系，并尽量利用多卡并行结果汇总。
- 文件：evaluators/metrics.py, evaluators/spectrum_evaluator.py, utils/plotting.py, configs/eval/spectrum_eval.yaml, README.md, docs/notes.md, docs/logs/2026-03.md
- 修改：
  - 新增 `DistributionPlotAccumulator`，各 rank 本地累计计数后再统一 all-reduce
  - 新增 2x2 评测统计总览图：
    - `R-RMSE vs Count`
    - `T-RMSE vs Count`
    - `Generated Length vs Target Length`
    - `Sequence Loss vs Count`
  - 新增配置项 `evaluation.save_distribution_plots` 与 `evaluation.distribution_plots.*`
  - README 同步补充 `plots/summary/<split>_distribution.png` 输出路径
- 验证：
  - `D:\anaconda\envs\oneday\python.exe -c "import compileall; ..."`，结果 `overall: True`
  - `D:\anaconda\envs\oneday\python.exe -c "from utils.plotting import save_eval_distribution_summary ..."`，结果成功生成测试图；随后已删除测试文件
- 提交：`git commit -m "feat: add evaluation distribution summary plots"`

## 2026-03-27 减少同步点与批量统计
- 需求：继续优化 GPU 空转问题，减少评测/训练阶段的强制同步点，并把样本级统计改成批量累计。
- 文件：evaluators/metrics.py, evaluators/spectrum_evaluator.py, trainers/spectral_sft_trainer.py, configs/eval/spectrum_eval.yaml, README.md, docs/notes.md, docs/logs/2026-03.md
- 修改：
  - 训练阶段去掉每个 batch 的 `.cpu().item()` 同步，改为先在 GPU 上累计，再按 `log_interval` 或 epoch 末同步
  - `MetricAccumulator` / `DistributionPlotAccumulator` 新增 `update_batch`
  - 评测阶段改为批量累计序列误差、光谱误差和长度统计
  - 默认关闭逐样本折线图 `evaluation.save_plots: false`，保留汇总统计图
- 验证：
  - `D:\anaconda\envs\oneday\python.exe -c "import compileall; ..."`，结果 `overall: True`
  - `D:\anaconda\envs\oneday\python.exe -c "from evaluators.metrics import ..."`，结果批量累计计数正常
- 提交：`git commit -m "perf: reduce sync points and vectorize evaluation stats"`

## 2026-03-27 SFT 训练链路继续提速
- 需求：继续检查并优化 SFT 代码，重点降低训练时验证开销、减少 Python 对象构造，并参考前面对评测线路的 GPU 利用率分析。
- 文件：configs/sft/spectral_sft.yaml, evaluators/spectrum_evaluator.py, losses/spectrum_loss.py, trainers/spectral_sft_trainer.py, docs/notes.md, docs/logs/2026-03.md
- 修改：
  - SFT 训练中的验证默认关闭 `save_plots` 与 `save_distribution_plots`，并将验证 `batch_size/scoring_batch_size` 调到 `128`
  - `evaluate_generated_structures(...)` 新增批量数组快路，训练阶段可以只拿 `spectrum_losses` 和 `ok_mask`
  - `SpectrumEvaluator` 增加真值结构 token id 缓存，减少每轮验证重复编码
  - `SpectrumEvaluator.evaluate(...)` 使用 `torch.inference_mode()`，在不保存样本/图片时跳过逐样本结果构造
  - `SpectralSFTTrainer._train_batch(...)` 改为直接消费批量光谱损失数组，减少 list/dict 到 tensor 的往返
- 验证：
  - `D:\anaconda\envs\oneday\python.exe -m compileall trainers evaluators losses runners models datasets utils`
  - `@' ... evaluate_generated_structures(...) ... '@ | D:\anaconda\envs\oneday\python.exe -`
- 结果：通过
- 提交：`git commit -m "perf: reduce spectral sft validation overhead"`

## 2026-03-27 Generation / Scoring 热路径优化
- 需求：继续优化 `generation.py` 和 `scoring.py`，减少 Python 循环与显存重复分配，并让 batch 进度条显示当前子阶段。
- 文件：models/optogpt/generation.py, models/optogpt/scoring.py, trainers/spectral_sft_trainer.py, evaluators/spectrum_evaluator.py, docs/notes.md, docs/logs/2026-03.md
- 修改：
  - `generation.py`
    - 解码张量改为整块预分配，避免每一步 `torch.cat`
    - 用张量维护 `generated_lengths/terminated_by_eos/step_logprobs`
    - 将 token 字符串恢复推迟到解码结束后统一处理
    - 新增下三角 mask 缓存
  - `scoring.py`
    - teacher forcing 张量改为批量扁平化回填，避免逐样本 `torch.tensor(...)`
    - 无梯度分支改为 `torch.inference_mode()`
    - 输出张量改为一次性预分配后按 chunk 写回
    - 新增下三角 mask 缓存
  - 训练与评测进度条新增 `stage` 字段，显示 `generate / tmm / score / backward / write`
- 验证：
  - `D:\anaconda\envs\oneday\python.exe -m compileall trainers evaluators losses runners models datasets utils`
  - `@' ... sequence_logprobs_multi_target_batch_tensor(...) ... '@ | D:\anaconda\envs\oneday\python.exe -`
  - `@' ... generate_structures_for_targets(...) ... '@ | D:\anaconda\envs\oneday\python.exe -`
- 结果：通过
- 提交：`git commit -m "perf: optimize generation and scoring hot paths"`

## 2026-03-27 训练阶段 epoch 曲线与 R/T 汇总
- 需求：在 spectral SFT 训练结束后自动绘制按 epoch 汇总的训练/验证曲线，包含训练集与验证集的 `R-RMSE`、`T-RMSE`、`sequence_loss` 变化；同时保持多卡兼容。
- 文件：losses/spectrum_loss.py, evaluators/metrics.py, evaluators/spectrum_evaluator.py, trainers/spectral_sft_trainer.py, utils/plotting.py, configs/sft/spectral_sft.yaml, docs/notes.md, docs/logs/2026-03.md
- 修改：
  - `spectrum_loss.py`
    - 批量评估结果新增 `r_rmse`、`t_rmse` 辅助数组
    - 无效结构、非物理解、TMM 失败样本统一回填 `R/T` 惩罚值
  - `metrics.py`
    - `MetricAccumulator` 新增 `R-RMSE / T-RMSE` 的和、均值、最值
    - 多卡 reduce 时一并归并 `R/T` 指标
  - `spectrum_evaluator.py`
    - 汇总 CSV 新增 `mean_r_rmse`、`mean_t_rmse`
    - 评测进度条新增累计 `r/t`
    - 样本落盘时补写 `r_rmse`、`t_rmse`
  - `spectral_sft_trainer.py`
    - 训练 epoch 汇总改为按样本数加权，避免简单平均 batch 均值带来偏差
    - 每个 epoch 额外写出 `mean_train_sequence_loss`、`mean_train_r_rmse`、`mean_train_t_rmse`、`val_r_rmse`、`val_t_rmse`
    - 训练结束后由主进程保存 `plots/epoch_summary.png`
  - `plotting.py`
    - 新增 `save_sft_epoch_summary_plot(...)`
  - `spectral_sft.yaml`
    - 新增 `training.save_epoch_plots: true`
- 验证：
  - `D:\anaconda\envs\oneday\python.exe -m compileall trainers evaluators losses utils runners models datasets`
  - `@' ... MetricAccumulator + save_sft_epoch_summary_plot smoke test ... '@ | D:\anaconda\envs\oneday\python.exe -`
  - `@' ... evaluate_generated_structures(..., return_aux_arrays=True) smoke test ... '@ | D:\anaconda\envs\oneday\python.exe -`
- 结果：通过
- 提交：`git commit -m "feat: add spectral sft epoch summary curves"`

## 2026-03-27 修复 spectral SFT 的 R/T 统计崩溃
- 需求：修复多卡 spectral SFT 在训练早期 `stage=tmm` 崩溃的问题。当前异常由新增 `R-RMSE / T-RMSE` 统计触发。
- 文件：losses/spectrum_loss.py, docs/notes.md, docs/logs/2026-03.md
- 修改：
  - `spectrum_loss.py`
    - 修复 `evaluate_generated_structures(...)` 中 `r_rmse / t_rmse` 的计算方式
    - 不再把单段 `reflection / transmission` 曲线传给只接受拼接 `[R..., T...]` 光谱的 `spectrum_error(..., metric='r_rmse/t_rmse')`
    - 改为直接按单段曲线计算 RMSE
- 原因：
  - `reflection / transmission` 是单段 `71` 点曲线
  - `physics/spectrum.py::spectrum_error(...)` 的 `r_rmse/t_rmse` 分支仍按拼接光谱解析
  - 因此会触发 `光谱长度必须为偶数，当前长度为 71`
- 验证：
  - `D:\anaconda\envs\oneday\python.exe -m compileall losses evaluators trainers`
  - `@' ... split_rt_spectrum + 单段 RMSE smoke test ... '@ | D:\anaconda\envs\oneday\python.exe -`
- 结果：通过
- 提交：`git commit -m "fix: handle single-channel rt rmse in spectral sft"`

## 2026-03-30 新增 SFT 前后 checkpoint 的 t-SNE 对比脚本
- 需求：阅读 `analysis_optogpt.ipynb` 中的 t-SNE 代码，并新增独立 Python 入口，对比 SFT 前后模型的隐藏表示。
- 文件：runners/run_tsne_compare.py, docs/notes.md, docs/logs/2026-03.md
- notebook 结论：
  - `cell 34` 用 `model.fc(...)` 提取光谱隐藏表示，并根据 checkpoint 的 `struc_word_dict` 构造 `mat_index`
  - `cell 35` 对结构隐藏表示与光谱隐藏表示拼接后做 `MinMaxScaler + TSNE`
  - `cell 36` 按材料类别和光谱点绘制散点图
- 修改：
  - 新增 `run_tsne_compare.py`
    - 同时加载 before / after 两个 checkpoint
    - 提取结构 token embedding 与光谱 `fc` 隐藏表示
    - 在共享数据上执行一次联合 t-SNE，再拆成 before / after 两个 panel
    - 输出 `tsne_compare.png`、`tsne_points.npz`、`metadata.json`
    - 保持 notebook 中的材料顺序、颜色和图例语义
  - 额外修复 `numpy 2.x` 下 `ndarray.ptp()` 被移除的问题，改为 `np.ptp(...)`
- 验证：
  - `D:\anaconda\envs\oneday\python.exe runners/run_tsne_compare.py --help`
  - `D:\anaconda\envs\oneday\python.exe runners/run_tsne_compare.py --before-checkpoint model/optogpt.pt --after-checkpoint model/optogpt.pt --spectrum-path dataset/Spectrum_test.npy --max-spectra 8 --batch-size 8 --n-iter 250 --output-dir outputs/_debug --name tsne_smoke --device auto`
- 结果：通过
- 提交：`git commit -m "feat: add tsne comparison runner for sft checkpoints"`

## 2026-03-31 修复 spectral SFT 中 rollout 与 scoring 的策略前向不一致
- 需求：检查 spectral SFT 训练中 rollout 采样与重算 `logprob` 是否使用同一个策略；若训练态 `dropout` 会导致两次前向对应不同策略，则修复。
- 文件：trainers/spectral_sft_trainer.py, configs/sft/spectral_sft.yaml, docs/notes.md, docs/logs/2026-03.md
- 分析：
  - 原训练流程在每个 epoch 开头先把模型切到 `train()`
  - batch 内 rollout 使用 `torch.inference_mode()`，但不会关闭训练态 `dropout`
  - 随后 scoring 重新前向并保留 autograd，也会使用另一份训练态 `dropout` mask
  - 因此 sampled action 与反传 `logprob` 并不严格来自同一个策略前向，额外增加方差
- 修改：
  - `spectral_sft_trainer.py`
    - 新增 `training.policy_forward_mode` 读取与合法性校验，仅支持 `train/eval`
    - 在 `_train_batch(...)` 内让 rollout 和 score 两次前向共用同一个策略前向模式
    - 默认使用 `eval` 前向模式关闭 `dropout`，但 scoring 仍保留 autograd 与 `backward()`
    - batch 结束后恢复进入 `_train_batch(...)` 前的模块模式
    - 训练 epoch 开头改为对 `self.model.model` 调用 `train()`，兼容 DDP 包装
  - `spectral_sft.yaml`
    - 新增 `training.policy_forward_mode: eval` 及用途注释
- 验证：
  - `python -m compileall trainers models runners`
- 结果：待验证
- 提交：`git commit -m "fix: align spectral sft rollout and scoring policy mode"`

## 2026-03-31 将 spectral SFT 训练路径升级为 spectral GRPO
- 需求：把现有 spectral SFT 训练路径改造成真正的 GRPO，实现 rollout/update 共用同一 policy 定义，按同一 target spectrum 的候选组计算 advantage，并保持现有 evaluator/TMM/checkpoint 体系可复用。
- 文件：README.md, configs/grpo/spectral_grpo.yaml, configs/sft/spectral_sft.yaml, datasets/optogpt_dataset.py, datasets/splits.py, evaluators/spectrum_evaluator.py, losses/__init__.py, losses/grpo_loss.py, models/optogpt/__init__.py, models/optogpt/generation.py, models/optogpt/policy.py, models/optogpt/scoring.py, runners/run_grpo.py, runners/run_spectral_sft.py, trainers/__init__.py, trainers/grpo_trainer.py, trainers/spectral_sft_trainer.py, utils/plotting.py, docs/notes.md, docs/logs/2026-03.md
- 修改：
  - `models/optogpt/policy.py`
    - 新增 rollout / update 共用的策略分布定义
    - 统一实现 `temperature / top-k / top-p / greedy / sample`
  - `models/optogpt/generation.py`
    - rollout 改为从共享 policy 分布采样
    - 记录 `policy_logprobs` 与 `sequence_policy_logprob` 作为 `old_logprobs`
    - 多候选展开顺序改为按 target 分组，便于组内 advantage 计算
  - `models/optogpt/scoring.py`
    - `sequence_logprobs_multi_target_batch_tensor(...)` 新增 `decode_config`
    - 支持按 rollout policy 定义重算 current logprobs
  - `losses/grpo_loss.py`
    - 新增序列级 logprob 聚合、组内 advantage、clipped surrogate、clip fraction、approx KL
  - `trainers/grpo_trainer.py`
    - 用 `reward = -spectrum_loss`
    - 按同一 target 的候选组做 `center/zscore` advantage
    - 使用 PPO/GRPO-style clipped objective 更新模型
    - 支持变长序列、EOS、padding mask
    - 保留 gradient accumulation，并修正累积梯度时的 loss 缩放
    - rollout / score 默认共用 `policy_forward_mode: eval`
  - `runners/run_grpo.py`
    - 新增 GRPO 主入口
  - `runners/run_spectral_sft.py`, `trainers/spectral_sft_trainer.py`, `configs/sft/spectral_sft.yaml`
    - 改为兼容旧路径的薄封装或兼容配置
  - `configs/grpo/spectral_grpo.yaml`
    - 新增 `sampling.rollout` 与 `sampling.eval`
    - GRPO 默认训练 policy 为 `sample + top_k=0 + top_p=1.0 + temperature=1.0`
    - 新增 `group_size / clip_epsilon / advantage_mode / advantage_eps / scoring_batch_size`
    - rollout / scoring / TMM 默认 batch 调大到 `4096`
  - `evaluators/spectrum_evaluator.py`
    - 评测改为对 `self.model.model` 切 `eval()`，兼容 DDP
  - `utils/plotting.py`
    - 新增 `save_grpo_epoch_summary_plot(...)`
    - 保留 `save_sft_epoch_summary_plot(...)` 兼容转发
- 验证：
  - `python -m compileall trainers models runners losses evaluators utils datasets`
  - `@' ... load_yaml_config("configs/grpo/spectral_grpo.yaml") ... '@ | python -`
  - `@' ... load_yaml_config("configs/sft/spectral_sft.yaml") ... '@ | python -`
  - `python runners/run_grpo.py --help`
- 结果：
  - `compileall` 通过
  - 两份 YAML 配置均可正常加载
  - 当前终端默认 `python` 缺少 `torch`，因此未完成运行态 smoke
- 提交：`git commit -m "feat: replace spectral sft with grpo training path"`
